# Riddra Progress

## Current status

Project stage: `Phase 20 build complete · private beta hardening, durable backend closure, and deferred public-launch planning ongoing`

### Current tracker truth

- Working target: `Build / Private Beta Readiness` first, `Public Launch Readiness` second.
- Final repo-wide private-beta audit: `67/100`.
- Ready now: admin or ops visibility, plus honest deferred billing posture.
- Partially ready: auth, data durability, jobs or workflows, search, email or support, and deployment.
- Fake or placeholder: several continuity, communications, search-truth, and chart fallback lanes still rely on file-backed private-beta stores or seeded fallback behavior.
- Actual blockers still remaining: real auth rehearsal with bypass off, live Trigger.dev proof, live Meilisearch rebuild proof, live Resend send proof, provider-backed market refresh proof, and durable market-data verification in the real database.
- Private-beta critical path now keeps durable jobs or workflows, durable market-data storage, durable search, email or support delivery, auth or account persistence, and deployment hardening in scope.
- Deferred from the current critical path: Razorpay, subscription flows, payment proof, public marketing readiness, and broad-public smoke checks.
- Completed in the last 8 hours: persisted search query feedback and zero-result review loop across live `/search` plus admin `/admin/search-screener-truth`, subscriber workspace cross-module continuity stitch across `/account` and `/account/workspace`, subscriber-memory record-grid and activity-log consolidation across `/account/setup` or `/account/alerts` or `/account/watchlists` or `/account/screens` or `/account/inbox` or `/account/workspace` or `/account/brokers` or `/account`, protected account stat-grid and rule-list consolidation across `/account/access` or `/account/access/entitlements` or `/account/billing` or `/account/brokers` or `/account/consents` or `/account/support` or `/account/billing/lifecycle` or `/account/billing/recovery`, subscriber audit-section consolidation across `/account/access` or `/account/access/entitlements` or `/account/support` or `/account/billing` or `/account/billing/lifecycle` or `/account/billing/recovery` or `/account/brokers` or `/account/brokers/review` or `/account/consents`, account or workspace registry-section consolidation across `/account` or `/account/workspace` or `/account/alerts` or `/account/watchlists` or `/account/screens` or `/account/inbox`, front-door or auth or support or mobile truth-section consolidation across homepage or pricing or get-started or login or signup or help or contact or mobile-app, custom truth-stat support across indices or learn or reports or community or newsletter or webinars or wealth hubs, shared truth-section expansion across markets or tools or stocks or mutual-funds or IPO or ETF or search or charts or screener or Bank Nifty or Nifty 50 or Fin Nifty or Sensex or index-replay hubs plus stock-compare or mutual-fund-compare or sector-detail or fund-category-detail routes, shared public truth-section extraction across launch-readiness or chart or learn or report or SME IPO or legal routes, launch-readiness or SME IPO or learn-event detail or learn-track detail or legal privacy-policy or legal terms truth completion, learning and participation child-route truth expansion across course-detail or lesson or webinar-detail or webinar-register or webinar-replay or newsletter-track or community-program or mentorship-track pages, compare and category truth expansion across stock-compare or mutual-fund-compare or sector-detail or fund-category-detail pages, detail-route truth expansion across stocks or mutual-funds or IPO or tools or ETF or AIF or PMS or SIF detail pages, privacy and terms truth expansion, preset or chart-layout or mentorship or source-readiness truth expansion, screener and index workflow truth expansion, ETF or AIF or PMS or SIF truth expansion, stocks or mutual-funds or IPO truth expansion, search and charts truth expansion, subscriber access and broker audit expansion, subscriber-facing registry truth expansion, launch command-board write-through coverage, market-data admin action-path isolation cleanup, launch packet and canonical export isolation cleanup, editorial and search export isolation cleanup, provider and launch-decision export isolation cleanup, source and archive export isolation cleanup, preflight and QA export isolation cleanup, launch-evidence export isolation cleanup, launch-readiness export isolation cleanup, payment-readiness export isolation cleanup, provider-onboarding export isolation cleanup, support-ops export isolation cleanup, market-data registry isolation cleanup, market-history registry split, tracker changelog sync, subscriber-launch export isolation cleanup, launch-control audit consolidation, subscriber-launch registry split, external-activation registry consolidation, subscriber-launch audit hub expansion, subscription-lifecycle registry split, workspace alert audit folded into the shared workspace registry and surfaced on workspace or alerts or watchlists or screens or inbox, workspace continuity audit split, support-delivery audit split, billing registry split, notification-event registry split, entitlement registry split, broker continuity audit split, and localhost plus build-tracker recovery on the stable `next start` path.
- Pending for the next 2 hours: finish private-beta config inputs, run private-beta deployment proof across domain or auth or provider or support, keep closing the biggest durability lanes like workers or search or market history, tighten subscriber-memory truth, and keep the deferred public-launch lane documented without letting it displace the current beta work.

### Latest execution layer

- Prepared the hosted-proof phase after durable lane conversion by auditing auth continuity, Supabase public/admin reads, CMS public/admin boundaries, worker/runtime branching, and email/support assumptions; captured the exact pass/fail checklist, route/API proof set, and remaining blockers in `docs/HOSTED_PROOF_CHECKLIST.md`, including the still-open public `/admin` link leakage and hosted-runtime heuristic verification targets.
- Migrated `/learn/[slug]`, `/learn/events/[slug]`, `/learn/tracks/[slug]`, and `/newsletter/[slug]` off the older `Container` or `GlowCard` detail shell and onto the shared public product-page system, keeping the editorial information model intact while aligning breadcrumb rhythm, support-column discipline, truth presentation, and main-column section structure with the broader product shell.
- Started the broader product-completion phase after pausing flagship lane work by reviewing ETF/PMS/AIF/SIF plus learn/newsletter families, standardizing their public truth wording through one shared helper, softening visible preview-heavy copy on reports and adjacent education/distribution routes, and documenting the next hosted-readiness blockers in `docs/BROADER_PRODUCT_COMPLETION_CHECKLIST.md`.
- Standardized the public research design system across the live product families by aligning stock, mutual-fund, index, ETF, PMS, AIF, SIF, learn, and newsletter hubs plus the wealth-family detail shells to the current Tata Motors benchmark: lighter shared product cards, tighter board density, trimmed sidebars, anchor-safe section rendering where supported, calmer truth-state language, and removal of older dark `GlowCard` or `PublicSurfaceTruthSection` layout paths from those families.
- Tightened the private-beta runtime configuration story by making launch-mode resolution honor stored launch-config instead of an env default override, adding first-class `private_beta` mode handling across launch-state and go-or-no-go surfaces, moving Resend runtime truth onto env-backed readiness instead of a stale launch-config fallback, aligning support and admin identity helpers with shared runtime config, and updating the deployment runbook plus `.env.example` into one exact setup checklist for `.env.local` and launch-config values.
- Cleaned up the private-beta deployment hardening lane again by teaching payment-readiness and external-activation surfaces to mark later commercial billing work as `Deferred` instead of `Blocked`, then propagating that deferred status into the launch-config console, launch-control summary, external-activation registry, and launch-pending audit so Razorpay-only work no longer inflates the active private-beta blocker count.
- Ran a repo-wide private-beta readiness audit and pushed the blunt truth back into `/build-tracker`: the page now has a dedicated final audit section with a `67/100` private-beta score, explicit `ready now` or `partially ready` or `fake or placeholder` or `intentionally deferred` or `actual blocker` buckets, and a top-10 remaining-task list split into must-do-before-beta versus safe-to-defer-later lanes.
- Reworked private-beta deployment hardening into a real operator lane by expanding `/admin/deployment-readiness` into a full deployment checklist, config checklist, active blocker list, ready-now summary, deferred commercial lane, and five-step smoke-test runbook for sign in, market refresh, search, support email, and persistent account state; tightened `/admin/launch-config-console` so billing counts now separate private-beta delivery inputs from deferred Razorpay inputs; gated `/account/billing` into a read-only private-beta billing posture instead of preview invoice-edit flows; refreshed `/pricing` copy to stop implying live checkout; and documented the deployment runbook in `docs/PRIVATE_BETA_DEPLOYMENT.md`.
- Wired in Meilisearch as the real search engine for private beta, added a shared search-engine abstraction plus indexed document mapping from the existing route and content graph, enabled typo tolerance or synonyms or ranking rules, moved Trigger rebuilds onto the live search index, rewired `/search` plus autocomplete to read through that shared engine when configured, and documented the local setup plus reindex flow in `docs/MEILISEARCH_SEARCH.md`.
- Installed and configured Trigger.dev as the new durable worker layer, added family-based Trigger task structure under `/trigger`, rewired market-data sync and refresh plus portfolio reconciliation and notification follow-up onto queued durable jobs, added a new support follow-up queue and a durable search-index rebuild lane, surfaced recent support and search worker visibility inside the app, and documented the env setup plus local Trigger workflow in `docs/TRIGGER_DEV_JOBS.md` without touching the deferred billing lane.
- Added Resend-backed private-beta email delivery for support acknowledgements, support follow-up, public contact requests, notification summary emails, and account-change alerts; removed the main fake delivered-state path for email notification events; added reusable templates, delivery logging, and admin-safe email log exports. Live provider proof still depends on a real Resend API key and verified sender.
- Rebased the tracker, roadmap, config priorities, and readiness checklist onto the new `private beta / internal production-ready` target, adding explicit `Build / Private Beta Readiness` versus `Public Launch Readiness` separation, a new priority order, deferred billing and public-smoke wording, and roadmap phase updates so the current critical path no longer treats Razorpay, subscription proof, payment proof, or public marketing readiness as day-one blockers.
- Extracted shared subscriber-record-grid and subscriber-activity-log sections and rewired `/account/setup`, `/account/alerts`, `/account/watchlists`, `/account/screens`, `/account/inbox`, `/account/workspace`, `/account/brokers`, and the main `/account` hub onto them, so the subscriber-memory cluster no longer carries another broad layer of duplicated saved-item cards, recent-feed cards, activity logs, and route-map blocks route by route.
- Added one stitched subscriber workspace continuity model plus a protected continuity export, then surfaced that continuity view on `/account` and `/account/workspace`, so portfolio, broker, billing, entitlement, delivery, and support posture now sit next to workspace state instead of leaving the workspace lane isolated from the rest of the signed-in backend.
- Added one persisted search query feedback registry plus a protected admin export, then wired live `/search` renders into it and surfaced that feedback loop on `/admin/search-screener-truth`, and added a write-through zero-result review backlog with owners, proposed aliases, and route targets, so search misses can be tightened from tracked usage instead of staying as a planned analytics lane.
- Extracted one shared subscriber-rule-list section and expanded the existing subscriber stat-grid and route-link grid across `/account/access`, `/account/access/entitlements`, `/account/billing`, `/account/brokers`, `/account/consents`, `/account/support`, `/account/billing/lifecycle`, and `/account/billing/recovery`, so the protected account cluster no longer carries another layer of duplicated summary-card, rule-list, and action-link layout code route by route.
- Extracted one shared subscriber-audit section and rewired `/account/access`, `/account/access/entitlements`, `/account/support`, `/account/billing`, `/account/billing/lifecycle`, `/account/billing/recovery`, `/account/brokers`, `/account/brokers/review`, and `/account/consents` onto it, so the protected account audit cluster no longer maintains duplicated registry-summary cards and action-link blocks route by route.
- Extracted one shared subscriber-workspace registry section and rewired `/account`, `/account/workspace`, `/account/alerts`, `/account/watchlists`, `/account/screens`, and `/account/inbox` onto it, so the main subscriber-memory cluster no longer maintains duplicated registry-audit cards and action-link blocks route by route.
- Expanded the shared public truth-section refactor across the front door, member entry, support, and mobile surfaces by wiring `/`, `/pricing`, `/get-started`, `/login`, `/signup`, `/help`, `/contact`, and `/mobile-app` onto the same reusable auth, billing, support, and continuity block, while also teaching that component to handle compact one-card and two-card stat layouts cleanly.
- Upgraded the shared public truth-section so it can render route-specific stat cards, then wired that more flexible component into `/indices`, `/learn`, `/reports`, `/community`, `/newsletter`, `/webinars`, and `/wealth`, so those hubs no longer need bespoke inline truth and summary-grid code to stay honest about auth, billing, support, and continuity posture.
- Expanded the shared public truth-section refactor across the top-level market, discovery, chart, screener, index, compare, and category routes by wiring `/markets`, `/tools`, `/stocks`, `/mutual-funds`, `/ipo`, `/etfs`, `/search`, `/charts`, `/screener`, `/banknifty`, `/nifty50`, `/finnifty`, `/sensex`, `/index-replay`, stock compare, fund compare, sector detail, and fund-category detail onto the same reusable auth, billing, support, and continuity block, so the biggest public route cluster is easier to maintain and less likely to drift out of sync.
- Tightened the uncovered public-route lane by surfacing auth, billing, support, and continuity truth directly on `/launch-readiness`, `/ipo/sme`, learn event detail, learn track detail, legal privacy-policy, and legal terms, so those trust and learning surfaces no longer read softer than the real launch posture allows.
- Extracted the repeated public auth or billing or support or continuity block into one shared server component and rewired the newest chart, learn, report, SME IPO, launch-readiness, and legal batch onto it, so this truth layer can scale without more copy-paste drift.
- Tightened the learning and participation child-route lane by surfacing auth, billing, support, and continuity truth directly on course detail, lesson, webinar detail, webinar register, webinar replay, newsletter track, community program, and mentorship track routes, so those child content flows no longer read cleaner than the real launch posture allows.
- Tightened the compare and category lane by surfacing auth, billing, support, and continuity truth directly on stock-compare, mutual-fund-compare, sector-detail, and fund-category-detail routes, so those deeper decision flows no longer read cleaner than the real launch posture allows.
- Tightened the detail-route lane by surfacing auth, billing, support, and continuity truth directly on stock, mutual-fund, IPO, tool, ETF, AIF, PMS, and SIF detail pages, so the deeper research routes no longer read cleaner than the real launch posture allows.
- Tightened the privacy and terms lane by surfacing support, auth, billing, and continuity truth directly on `/privacy` and `/terms`, so those trust pages no longer read softer than the real launch posture allows.
- Tightened the preset, chart-layout, mentorship, and source-readiness lane by surfacing auth, billing, support, and continuity truth directly on `/scanner-presets`, `/trader-presets`, `/chart-layouts`, `/mentorship`, and `/source-readiness`, so those repeat-workflow, guided-learning, and activation-planning hubs no longer read cleaner than the real launch posture allows.
- Tightened the screener and index workflow lane by surfacing auth, billing, support, and continuity truth directly on `/screener`, `/banknifty`, `/nifty50`, `/finnifty`, `/sensex`, and `/index-replay`, so those market-intelligence and trader-review hubs no longer read cleaner than the real launch posture allows.
- Tightened the ETF, AIF, PMS, and SIF lane by surfacing auth, billing, support, and continuity truth directly on `/etfs`, `/aif`, `/pms`, and `/sif`, so those wealth-family hubs no longer read cleaner than the real launch posture allows.
- Tightened the stocks, mutual-funds, and IPO lane by surfacing auth, billing, support, and continuity truth directly on `/stocks`, `/mutual-funds`, and `/ipo`, so those asset-discovery hubs no longer read cleaner than the real launch posture allows.
- Tightened the search and charts lane by surfacing auth, billing, support, and continuity truth directly on `/search` and `/charts`, so those discovery and chart hubs no longer read cleaner than the real launch posture allows.
- Tightened the wealth and trust-discovery lane by surfacing auth, billing, support, and continuity truth directly on `/wealth`, `/fund-categories`, `/sectors`, and `/methodology`, so those discovery and trust hubs no longer read cleaner than the real launch posture allows.
- Tightened the creator and education distribution lane by surfacing auth, billing, support, and participation continuity truth directly on `/community`, `/newsletter`, `/webinars`, and `/courses`, so those public creator and education hubs no longer read cleaner than the real launch posture allows.
- Tightened the indices, learn, and reports lane by surfacing auth, billing, support, and continuity truth directly on `/indices`, `/learn`, and `/reports`, so those public hubs no longer read cleaner than the real launch posture allows.
- Tightened the markets and tools lane by surfacing auth, billing, support, and discovery continuity truth directly on `/markets` and `/tools`, so those public hubs no longer read cleaner than the real launch posture allows.
- Tightened the homepage and mobile lane by surfacing auth, billing, support, push, and continuity truth directly on `/` and `/mobile-app`, so those discovery-heavy public routes no longer read cleaner than the real launch posture allows.
- Tightened the public pricing and login lane by surfacing auth, billing, support, and access continuity truth directly on `/pricing` and `/login`, so those entry pages no longer read cleaner than the real activation state allows.
- Tightened the public onboarding lane by surfacing auth, billing, support, feedback-inbox, and onboarding continuity truth directly on `/get-started` and `/signup`, so those entry pages no longer read like static launch copy detached from the real subscriber handoff posture.
- Tightened the public help lane by surfacing support-delivery, billing, feedback-inbox, and signed-in continuity truth directly on `/help` and `/contact`, so public support guidance is no longer disconnected from the stitched support posture already used on account and admin routes.
- Tightened support and canonical-intake scope again by adding a protected subscriber support-registry export for `/account/support`, scoping support-registry hrefs away from admin-only desks on the subscriber path, and moving canonical asset-intake template downloads onto a dedicated protected admin route instead of the old generic template export.
- Tightened subscriber audit truth again by surfacing entitlement registry coverage on the main access model page and broker registry coverage on broker review, so those stitched backend audits are no longer hidden mostly in sub-routes or admin.
- Tightened subscriber-facing truth by surfacing the workspace registry on the main account hub, the subscriber activation packet on account setup, and support-ops registry coverage on account support, so those backend audits are no longer hidden mostly in admin or sub-route silos.
- Tightened the launch command-board cluster by adding write-through readiness panels to launch-day-console, launch-evidence-board, and public-launch-backlog, so the last top-level launch summary pages can now append real revision rows instead of behaving only like summary dashboards.
- Tightened the market-data operator cluster by adding protected admin sample-payload, validate, refresh, ingest, and provider-sync routes, then repointing the admin tester, market-data desk, provider-onboarding, source-jobs, and external-activation surfaces to those admin action paths instead of generic live-data endpoints.
- Tightened the launch packet and canonical coverage cluster by moving release-checks, smoke-test-journeys, subscriber-activation-packet, launch-blocker-ledger, launch-rehearsal-packet, canonical-coverage, and canonical-compare-coverage onto dedicated protected admin exports, so those operator desks no longer depend on generic launch-packet or canonical export paths.
- Tightened the editorial and search cluster by moving reference-parity, ai-ops, conversion-path-audit, revisions, and search-screener-truth registries onto dedicated protected admin exports, so those operator desks no longer depend on generic editorial or search export paths.
- Tightened the provider and launch-decision cluster by moving provider-configs, provider-fallbacks, launch-approvals, and go-no-go registries onto dedicated protected admin exports, so those launch-control desks no longer depend on generic provider or launch-decision export paths.
- Tightened the source and archive cluster by moving source-mapping, source-jobs, and research-archive registries onto dedicated protected admin exports, so those operator desks no longer depend on generic source or archive export paths.
- Tightened the preflight and QA cluster by moving preflight-checklist, public-launch-qa, mobile-qa, launch-day-runbook, chart-verification, reliability, and placeholder-honesty exports onto dedicated protected admin routes, so those launch-safety desks no longer depend on generic QA export paths.
- Tightened the launch-evidence cluster by moving the shared proof export onto one dedicated protected admin route and repointing launch-evidence-board, go-no-go, public-launch-qa, launch-signoff-packet, and release-gate-board to it, so those launch-proof desks no longer depend on a generic evidence export path.
- Tightened the communication-readiness, trust-signoff, and announcement-readiness lanes together by moving their registries onto dedicated protected admin exports and removing the stale generic CSV routes, so those launch-facing audits no longer depend on shared export paths.
- Tightened the payment-readiness lane again by moving its billing-activation registry onto a dedicated protected admin export and removing the stale generic CSV route, so payment audit no longer depends on a shared launch-facing export path.
- Tightened the provider-onboarding lane again by moving its activation registry onto a dedicated protected admin export and removing the stale generic CSV route, so provider audit no longer depends on a shared launch-facing export path.
- Tightened the support-ops lane again by repointing the admin support desk to the dedicated protected admin support-ops registry export and removing the stale generic CSV route, so operator audit no longer depends on a shared export path.
- Tightened the market-data lane again by adding dedicated protected admin derivatives and market-data-target registry exports, repointing the market-data desk to those admin routes, and removing the stale generic CSV endpoints so the operator audit lane no longer depends on shared export paths.
- Tightened the market-history lane by adding a dedicated protected admin market-history registry export and repointing the market-data admin desk to it, then removing the stale generic CSV route so retained-series audit no longer depends on a shared export path.
- Tightened tracker truth again by teaching the real-progress strip to record fresh execution snapshots even when the rounded overall percent stays flat, so the top changelog no longer freezes at the last numeric move.
- Tightened the subscription-lifecycle lane by splitting the lifecycle registry into a protected subscriber export for account jobs plus recovery rows and a separate protected admin export for ops automation, then repointing billing lifecycle, billing recovery, and admin payment-events to those scoped audit surfaces instead of one mixed queue export.
- Tightened the workspace lane by folding alert-preference and alert-feed rows into the shared workspace registry, then surfacing that stronger alert audit coverage on both the workspace hub and the alert-preferences route so alert posture is no longer split between the alerts page and a partial workspace export.
- Tightened the external-activation lane by adding one exportable registry that combines launch-config input groups with outside-the-code blocker rows, then surfacing that same activation truth on both the external-activation desk and the launch-config console so remaining provider, billing, support, compliance, and go-live inputs are no longer split between setup forms and blocker cards.
- Tightened the subscriber-launch lane again by adding a dedicated protected admin subscriber-launch registry export and repointing the subscriber-launch-readiness desk to it, so launch-side subscriber audit no longer depends on the shared generic export path.
- Tightened the launch-control desk by surfacing both the external-activation registry and the protected admin subscriber-launch registry directly on the central launch page, so top-level launch review no longer depends only on blocker cards plus separate drill-down desks for the two biggest remaining audit lanes.
- Tightened the subscriber-launch export boundary again by removing the now-orphaned shared CSV route, so the launch audit lane no longer keeps a stale duplicate export path after moving launch ops onto the protected admin export.
- Tightened the workspace lane again by surfacing the shared workspace registry directly on watchlists, saved screens, and inbox, so those subscriber-memory routes can now expose the same exportable backend audit slice as the main workspace and alerts pages instead of leaving that registry truth hidden one step away.
- Tightened the subscriber launch desk again by surfacing billing, entitlement, lifecycle, delivery, and portfolio continuity registries directly on subscriber-launch-readiness, so launch-side subscriber review no longer has to bounce between billing, entitlements, payment-events, delivery-layers, and portfolio-exceptions just to inspect the main backend truth slices.
- Tightened the workspace lane again by adding a dedicated admin workspace-registry export and surfacing that stitched audit summary directly on subscriber-launch-readiness, so launch-side workspace review no longer has to route back through subscriber workspace pages just to inspect the same backend slice.
- Tightened the support-delivery lane again by adding a dedicated admin support-ops registry export and surfacing that stitched audit summary directly on subscriber-launch-readiness, so launch-side support review no longer has to route back through the standalone support desk just to inspect the same backend slice.
- Tightened the broker-sync lane again by adding a dedicated admin broker-sync registry export and surfacing that stitched audit summary directly on subscriber-launch-readiness, so launch-side broker review no longer has to route back through the subscriber broker workspace just to inspect the same backend slice.
- Tightened the portfolio lane again by adding a dedicated admin portfolio-registry export and surfacing that stitched audit summary directly on the portfolio-exceptions desk, so operator-side mismatch and reconciliation work no longer has to route back through the subscriber portfolio registry just to inspect the same backend slice.
- Tightened the billing lane by giving the subscriber billing workspace its own stitched registry snapshot and CSV export while moving full billing-ledger download onto a dedicated protected admin route, then surfacing those registry summaries directly inside account billing and admin billing-ledger so invoice and payment-event audit history is no longer split behind one generic export path.
- Tightened the notification-event lane by adding one stitched registry for consent-aware channel routes and delivery events, then surfacing scoped subscriber export plus protected admin export on the consent center and delivery-layers desks so messaging history is no longer trapped inside page-only panels.
- Stabilized the root auth boundary by moving the Supabase session gate back onto `middleware.ts`, which lines the build output up with Next’s expected middleware trace artifacts again instead of tripping production builds on missing `proxy` trace files during finalization.
- Deepened the mutual-fund demo lane by adding a rolling-return lens plus an overlap-aware peer watchlist on `/mutual-funds/[slug]`, so single-fund pages now surface return consistency, visible portfolio differentiation, and better allocator handoffs without forcing every shortlist decision into the compare route.
- Tightened the entitlement-sync lane by giving the subscriber entitlement audit its own scoped registry snapshot and CSV export while moving full entitlement-registry download onto a dedicated protected admin route, so access history no longer depends on admin-only truth or a generic shared endpoint.
- Deepened the subscription-lifecycle lane by adding one exportable lifecycle registry across account jobs, account recovery rows, and ops automation, then surfacing that stitched audit view inside account billing lifecycle, account billing recovery, and admin payment-events instead of leaving the queue split across separate pages.
- Deepened the broker-sync lane by adding persisted linked broker accounts with create/remove APIs, registry coverage, and dedicated account-linkage panels on `/account/brokers`, so account linkage no longer has to be inferred only from rollout targets and queued sync runs.
- Wired the launch-config console and the admin dashboard into the shared editorial revision lane by adding write-through readiness actions on both setup desks, so launch-input posture and the main backend entry point no longer sit as a silent form surface or static jump page.
- Wired the source-entry, source-job, source-mapping, and research-archive cluster into the shared editorial revision lane by adding write-through readiness actions on source-entry-console, source-jobs, source-mapping-desk, and research-archive, so queue posture, source-stack posture, archive continuity, and manual-source cleanup no longer sit as static intake or archive consoles.
- Wired the market-data and search-truth cluster into the shared editorial revision lane by adding write-through readiness actions on market-data and search-screener-truth, so provider-stack posture, chart-truth posture, route-index posture, and screener-truth changes no longer sit as static operator reports or audit desks.
- Wired the source-trust and AI-grounding cluster into the shared editorial revision lane by adding write-through readiness actions on ai-ops, knowledge-ops, and sources, so AI posture, retrieval-grounding posture, and source-registry trust changes no longer sit as static console or registry views.
- Wired the registry, lifecycle, execution, and portfolio-exception cluster into the shared editorial revision lane by adding write-through readiness actions on asset-registry, db-activation, lifecycle, phase-execution-board, and portfolio-exceptions, so those identity, rollout, and trust desks no longer sit as static admin runbooks.
- Tightened search and screener truth continuity again by making `/screener` preserve live query, sector, truth-state, compare-only, saved-stack, metric-view, and sort state in the URL while also extending smart-search screener handoffs with intent-derived metric-group and ranking defaults, so search-led shortlist links now reopen the same decision-ready screener view instead of dropping users into an unshareable local-only state.
- Wired the module-governance cluster into the shared editorial revision lane by adding write-through readiness actions on module-catalog, module-installer, module-presets, module-activation, operator-controls, and integration-marketplace, so those expansion-contract and operator-configuration desks no longer sit as static planning boards.
- Wired the architecture, lineage, asset-memory, intake, field-pack, design-system, and localization cluster into the shared editorial revision lane by adding write-through readiness actions on platform-architecture, data-lineage, asset-memory, canonical-asset-intake, field-packs, design-system, localization-studio, and language-rollouts, so those scale, audit, schema, polish, and localization desks no longer sit as static guidance boards.
- Wired the recovery, replay, distribution, starter-kit, setup-playbook, and wealth-ops cluster into the shared editorial revision lane by adding write-through readiness actions on recovery-journeys, replay-memory, distribution-ops, starter-kits, setup-playbook, and wealth-products, so those launch and expansion desks no longer sit as static ops or planning boards.
- Wired the creator and education operations cluster into the shared editorial revision lane by adding write-through readiness actions on creator-studio, education-library, learning-paths, media-library, and mentorship-ladders, so creator/media planning and guided-learning posture no longer sit as static content-ops boards.
- Wired the mobile continuity, lifecycle governance, conversion-truth, parity-audit, and reliability cluster into the shared editorial revision lane by adding write-through readiness actions on mobile-journeys, journey-governance, experience-audit, conversion-path-audit, reference-parity, and reliability-ops, so those launch-facing audit desks no longer sit as static planning or checklist boards.
- Wired the CRM and lifecycle planning cluster into the shared editorial revision lane by adding write-through readiness actions on crm-ops, campaign-engine, campaign-insights, cohort-ops, lifecycle-campaigns, and segment-playbooks, so growth, retention, recovery, and cohort-planning posture no longer sit as static planning boards.
- Wired the access and monetization planning cluster into the shared editorial revision lane by adding write-through readiness actions on access-governance, api-access, and subscription-matrix, so access posture, provider-credential collection, and entitlement-plan mapping no longer sit as static planning boards.
- Wired the beta-control cluster into the shared editorial revision lane by adding write-through readiness actions on beta-gate, beta-invite-plan, beta-feedback, beta-metrics, and beta-triage, so controlled-beta scope, invite posture, feedback prioritization, health metrics, and triage rules no longer sit as static launch planning boards.
- Tightened compare-route handoff truth again by giving stock and mutual-fund compare candidates an explicit peer-detail target instead of deriving it from canonical compare URLs, so “Open detail” actions now stay on the intended peer even when canonical left/right ordering flips for the compare route; this run also switched `npm run build` onto a guarded wrapper that clears stale `.next/server` artifacts before production builds, removing the recurring local `ENOTEMPTY` cleanup failure from the verification path.
- Tightened the portfolio backend lane again by adding richer import-run provenance, latest-checkpoint summaries, and before-or-after unresolved-row diffs on reconciliation checkpoints, so `/portfolio/import` no longer stores confirmation history without showing what changed between checkpoints.
- Tightened chart-launch safety again by removing the hosted TradingView fallback from stock chart routes when symbol-bound OHLCV is missing, replacing mutual-fund hosted proxy charts with an explicit benchmark-handoff card, and moving chart stability from a blocked launch-QA item into a genuine in-progress verification lane.
- Tightened launch-readiness truth again by teaching runtime config to ignore placeholder env values and by making the real progress score count completed cutover checklist items instead of only raw auto-status, so `/build-tracker` now reflects genuine activation proof more accurately.
- Tightened the portfolio backend lane again by adding persisted reconciliation checkpoints with a dedicated API, operator panel, recent-checkpoint history, and registry coverage, so `/portfolio/import` no longer treats user-confirmed reconciliation as only row-by-row review activity.
- Tightened stock and mutual-fund discovery polish again by fixing the public hub, sector, and fund-category showcase compare sections to pull multiple ranked compare routes instead of accidentally capping themselves to one matchup, while also upgrading the main stock and fund hub top folds with route-truth, category or sector breadth, factsheet-evidence, and strongest-handoff summaries so demo users can understand what to open next without drilling into the grid first.
- Stabilized local launch ops and tracker truth together by changing the guarded dev runner into a preflight-only tsconfig normalizer with artifact cleanup instead of a runtime rewrite loop, stopping the local dev entrypoint from forcing webpack, adding a lightweight localhost healthcheck script, and moving `/build-tracker` onto a computed file-backed progress snapshot plus percent-change history so the top number, timestamp, and changelog now refresh from live tracker buckets instead of a stale hardcoded block.
- Tightened local launch-ops continuity again by teaching the guarded dev runner to refuse duplicate local server instances for the same workspace, persist the active localhost runtime state to disk, and feed `/build-tracker` with a dedicated local-operator-runtime panel showing the guarded URL, process state, timestamps, and tracked-route health results from the new healthcheck path.
- Added a stable localhost fallback for launch operations by exposing `local:stable` in package scripts and updating the tracker/runtime narrative to point operators toward a clean production-build plus `next start` path whenever the dev runtime starts dropping GET-only `.next/dev` manifests again.
- Tightened compare-route truth across the main stock, mutual-fund, search, smart-search, and screener discovery surfaces by canonicalizing ranked compare handoffs before links are rendered, so demo traffic now lands on one stable stock or fund matchup URL instead of leaking duplicate left/right variants from ranked hub and catalog flows.
- Tightened the screener into a cleaner decision-ready top fold by adding route-selection cards for best current stock, strongest compare handoff, active truth posture, and pending-lane honesty inside `ScreenerWorkspace`, so filtered or searched results now explain what to open next before users drop into the table and lower rationale cards.
- Tightened the stock and mutual-fund detail routes into cleaner demo-ready top folds by adding decision-starter card grids for truth state, compare handoff, route context, and evidence posture, while also restoring the missing explanatory notes inside the stock quote-context block so the most important route-truth cues now land above the fold instead of hiding deeper in the page.
- Tightened smart-search follow-through for direct stock matches again by adding a route-backed screener handoff whenever the same query also carries shortlist or factor-screen intent, so stock-first searches no longer lose the broader screening workflow after landing on one canonical company route.
- Polished the stock compare route into a stronger demo-ready decision surface by adding a top-fold verdict block, side-by-side best-fit cards, alternate compare-route rotation, and Link-based route handoffs on `/compare/stocks/[left]/[right]`, bringing the stock matchup flow closer to the richer mutual-fund compare experience instead of dropping users straight into the lower-page metric grid.
- Extended the source-entry backend lane again by adding delete support for index rows, route overrides, stock closes, stock OHLCV, fund NAV, fund factsheet evidence, and gold or silver history rows, then wiring `/admin/source-entry-console` to remove stale manual source rows from the shared store instead of leaving the operator desk save-only.
- Extended the entitlement override backend lane again by adding delete support to both subscriber and admin override APIs, switching admin overrides to `ops` actor context, and wiring `/account/access/entitlements` plus `/admin/entitlements` with dedicated override-management panels instead of leaving manual overrides as create-only writes.
- Extended the subscription-lifecycle backend lane again by adding a dedicated admin payment-events recovery-actions API, extending the shared lifecycle-ops store with persisted ops recovery rows, and wiring `/admin/payment-events` with create, update, and management panels for reminder, support, and fallback-review actions instead of leaving that desk with lifecycle jobs only.
- Tightened the source-job and archive-refresh backend lanes again by teaching the main admin routes to accept both create and update payloads, then pointing the create panels at those unified endpoints instead of keeping those operator desks split across separate `/create` routes.
- Tightened the AI persistence backend lanes again by teaching the main dataset, generation-run, and answer-packet admin routes to accept both create and update payloads, then pointing the AI create panels at those unified endpoints instead of keeping AI ops split across separate `/create` routes.
- Tightened the market-history and derivatives backend lanes again by teaching the main admin routes to accept both create and update payloads, then pointing the market-history, derivatives-snapshot, and derivatives-analytics create panels at those unified endpoints instead of keeping those desks split across separate `/create` routes.
- Tightened the search-index and research-archive backend lanes again by teaching the main admin routes to accept both create and update payloads, then pointing the search-index and research-archive create panels at those unified endpoints instead of keeping those desks split across separate `/create` routes.
- Extended the launch-proof backend lanes again by wiring `/admin/external-activation`, `/admin/external-activation-board`, `/admin/release-checks`, and `/admin/live-smoke-tests` with write-through readiness panels, so activation blockers, activation-board posture, release verification, and smoke-test proof no longer live only as static launch dashboards.
- Extended the readiness-governance backend lanes again by wiring `/admin/deployment-readiness`, `/admin/preflight-checklist`, `/admin/external-prerequisites`, `/admin/recovery-readiness`, `/admin/app-release-control`, and `/admin/demo-showcase-readiness` with write-through readiness panels, so deployment truth, last-pass signoff, off-repo prerequisites, rollback posture, native rollout planning, and showcase readiness no longer live only as static launch boards.
- Extended the operational launch backend lanes again by wiring `/admin/performance-qa`, `/admin/incident-response`, `/admin/observability`, `/admin/traffic-health`, `/admin/cache-discipline`, and `/admin/system-status` with write-through readiness panels, so runtime quality, incident posture, alerting visibility, traffic health, freshness control, and actual environment truth no longer live only as static operator boards.
- Extended the provider-governance backend lanes again by wiring `/admin/provider-configs`, `/admin/provider-fallbacks`, `/admin/provider-rollouts`, `/admin/provider-adapters`, `/admin/provider-switchboard`, and `/admin/market-data-playbook` with write-through readiness panels, so provider profiles, fallback posture, rollout control, adapter contracts, switchboard governance, and market-data execution steps no longer live only as static operator boards.
- Extended the launch-execution backend lanes again by wiring `/admin/push-readiness`, `/admin/credential-matrix`, `/admin/callback-matrix`, and `/admin/go-live-handoff` with write-through readiness panels, so push activation, exact env-key posture, auth callback truth, and launch handoff sequencing no longer live only as static operator boards.
- Extended the subscriber-launch backend lanes again by wiring `/admin/subscriber-launch-readiness`, `/admin/subscriber-activation-packet`, `/admin/consent-ops`, and `/admin/user-success` with write-through readiness panels, so subscriber gating, activation handoff, consent governance, and user-success operations no longer live only as static operator boards.
- Extended the beta-and-rehearsal backend lanes again by wiring `/admin/beta-command-center`, `/admin/beta-ops-runbook`, `/admin/launch-rehearsal-packet`, and `/admin/launch-signoff-packet` with write-through readiness panels, so beta gating, beta operating rhythm, public-launch rehearsal, and final signoff posture no longer live only as static launch briefings.
- Tightened the rollback backend lane again by teaching the main admin rollback route to accept create, update, and remove payloads, then pointing the rollback create and manage panels at that unified endpoint instead of keeping rollback on a leftover split `/create` route.
- Added a guarded local-dev wrapper script and switched `npm run dev` onto it so the repo now strips the unstable `.next/dev/types` tsconfig include while Next dev is running instead of letting localhost drift back into the same flaky state after every restart.
- Extended the editorial backend lane again by wiring the block-editor and editorial-workflows desks with write-through action panels that append real revision rows into the shared editorial memory store, so those pages now perform actual revision logging instead of only showing sample queues and registry readouts.
- Tightened the local verification path again by removing the unstable `.next/types` include from `tsconfig.json` itself and teaching both the dev guard plus postbuild normalizer to keep stripping `.next` type includes, so typecheck no longer collapses whenever Next rotates its generated route files.
- Extended the editorial backend lane again by wiring the CMS and publishing-calendar desks with write-through action panels that append real revision rows into the shared editorial memory store, so those two planning-heavy surfaces now also participate in the same change-control backend instead of staying read-only.
- Extended the editorial backend lane again by wiring the announcements and document-ops desks with write-through action panels that append real revision rows into the shared editorial memory store, so those two manual-content lanes now also participate in the same change-control backend instead of remaining descriptive-only queues.
- Extended the editorial backend lane again by wiring the content-overview desk with a write-through pipeline action panel that appends real revision rows into the shared editorial memory store, so asset queue moves and next-step decisions now also participate in the same change-control backend instead of remaining dashboard-only signals.
- Extended the editorial backend lane again by wiring the content-rollout, field-dictionary, and relationships desks with write-through action panels that append real revision rows into the shared editorial memory store, so rollout transitions, schema-governance changes, and graph-link mutations now also participate in the same change-control backend instead of remaining descriptive-only architecture cards.
- Extended the editorial backend lane again by wiring the content-model registry desk with a write-through action panel that appends real revision rows into the shared editorial memory store, so model-entity planning changes now also participate in the same change-control backend instead of remaining planning-only cards.
- Extended the editorial and launch-ops backend lanes again by wiring the announcement-readiness, support-ops, and launch-commitments desks with write-through action panels that append real revision rows into the shared editorial memory store, so messaging changes, support-lane changes, and launch-commitment mutations now also participate in the same change-control backend instead of remaining summary-only boards.
- Extended the editorial and launch-ops backend lanes again by wiring the launch-approvals, launch-blocker-ledger, and launch-decision desks with write-through action panels that append real revision rows into the shared editorial memory store, so owner signoff changes, blocker-ledger mutations, and go-live judgment changes now also participate in the same change-control backend instead of remaining summary-only launch boards.
- Extended the editorial and launch-ops backend lanes again by wiring the launch-owner-matrix, launch-day-runbook, launch-scorecard, and go-no-go desks with write-through action panels that append real revision rows into the shared editorial memory store, so owner-accountability changes, same-day rollout changes, readiness-truth changes, and final launch-call changes now also participate in the same change-control backend instead of remaining summary-only launch boards.
- Extended the editorial and launch-ops backend lanes again by wiring the launch-mode, launch-scope, launch-posture-board, release-gate-board, and activation-sequence desks with write-through action panels that append real revision rows into the shared editorial memory store, so posture changes, scope-boundary changes, posture-blocker changes, release-gate changes, and activation-order changes now also participate in the same change-control backend instead of remaining summary-only launch boards.
- Extended the editorial and launch-ops backend lanes again by wiring the launch-control and launch-owner-inbox desks with write-through action panels that append real revision rows into the shared editorial memory store, so blocker-owner mutations and owner-handoff changes now also participate in the same change-control backend instead of remaining static launch briefings.
- Tightened the broker-review and portfolio-review backend lanes again by teaching the main account routes to accept both create and update payloads, then pointing the broker-review and portfolio-review create panels at those unified endpoints instead of keeping those subscriber workflows split across separate `/create` routes.
- Retired the now-unused split `/create` API routes across broker review, portfolio review, AI persistence, source-job, archive-refresh, market-history, derivatives, search-index, and research-archive lanes so Next no longer ships duplicate endpoint surfaces after those desks were consolidated onto their main routes.
- Tightened the TypeScript validation path by removing the explicit `.next/dev/types` include from `tsconfig.json`, so clean backend verification no longer depends on flaky dev-only Next artifacts after cache resets.
- Reworked `/build-tracker` into a pending-first control surface by adding a launch decision table, a unified “everything still pending in one place” section, active-phase-only cards, and a separate collapsed completed-phases section so the tracker stops implying that build-complete phases mean launch-complete reality.
- Expanded `/build-tracker` again into a one-page launch control room by adding a non-technical launch command center, direct links to the exact setup and verification pages, merged launch-proof sections from readiness and QA, and pending-first summary cards so the domain-launch operator can track what is missing from one place.
- Tightened `/build-tracker` again so completed work no longer stays mixed into the active board: the execution lanes, backend references, import references, recent wins, and completed phase material are now pushed into collapsed archive sections at the bottom while the unfinished work stays expanded on top.
- Tightened `/build-tracker` one more time so the active phase cards and macro roadmap now render only phases that still have unfinished sub-items, and the live execution summaries pull from the first unfinished item instead of accidentally surfacing a completed row near the top.
- Simplified `/build-tracker` again so completed execution boards and roadmap sections disappear from the main scroll entirely when they have no unfinished work, leaving the top of the tracker focused on pending launch work while all completed build history stays tucked into the collapsed archive at the bottom.
- Tightened `/build-tracker` one more time so active phases now show only unfinished sub-items, the live launch checklist shows only unfinished steps, and the completed checklist and completed sub-items from still-active phases are both archived in collapsed sections at the bottom instead of leaking back into the main board.
- Expanded `/build-tracker` again with a real progress track at the top, including one explicit overall percentage, a last-updated timestamp, and a percent-change changelog so the tracker now shows the real project estimate instead of leaving that interpretation to chat.
- Extended the launch-activation backend lane again by wiring the domain-readiness, auth-activation, payment-readiness, provider-onboarding, and communication-readiness desks with a shared write-through readiness panel that appends real revision rows into the shared editorial memory store, so those launch-critical activation checklists stop living only as static status boards.
- Extended the launch-QA and trust backend lanes again by wiring the public-launch-qa, mobile-qa-matrix, and trust-signoff desks with the same shared write-through readiness panel, so final QA, compact-screen review, and trust signoff now append real revision rows instead of staying static checklists.
- Tightened the admin payment-events desk again by removing the last subscriber-endpoint fallback create panel, so event creation and cleanup on that page now stay fully inside the dedicated admin payment-events routes.
- Extended the billing-event backend lane again by introducing a dedicated admin billing-ledger events API, then wiring `/admin/billing-ledger` to create and remove payment-event rows through that admin route instead of routing the desk through subscriber billing endpoints.
- Extended the billing-invoice backend lane again by introducing a dedicated admin billing-ledger invoice API, then wiring `/admin/billing-ledger` to create and archive invoice rows through that admin route instead of routing the desk through the subscriber billing invoice endpoint.
- Extended the entitlement backend lane again by introducing dedicated admin entitlements sync-change and override APIs, then wiring `/admin/entitlements` to save and remove access rows through those admin routes instead of routing the audit desk through subscriber account endpoints.
- Extended the notification-event backend lane again by introducing dedicated admin delivery-layer channel and event APIs, then wiring `/admin/delivery-layers` to save and remove routing plus delivery rows through those admin routes instead of routing the operations desk through the subscriber consent endpoints.
- Extended the billing-event backend lane again by introducing a dedicated admin payment-events write/remove API, then wiring `/admin/payment-events` to create and remove event rows through that admin lane instead of routing the desk through the subscriber billing endpoint.
- Extended the alert-preference backend lane again by adding preference-removal APIs plus a dedicated management panel, then wiring `/account/alerts` to remove stale defaults from the shared workspace store instead of leaving that lane save-only after preference updates.
- Extended the consent-channel backend lane again by adding channel-route removal APIs plus a dedicated management panel, then wiring `/account/consents` and `/admin/delivery-layers` to remove stale routing rows from the shared notification-event store instead of leaving that lane save-only after channel updates.
- Extended the editorial revision backend lane again by adding revision-entry removal APIs plus a dedicated management panel, then wiring `/admin/revisions` to remove stale change-control rows from the shared editorial memory store instead of leaving the revision lane create-only after write-through logging.
- Extended the rollback backend lane again by adding rollback-scenario removal APIs plus a dedicated management panel, then wiring `/admin/rollback-center` and `/admin/revisions` to remove stale recovery rows from the shared editorial memory store instead of leaving that lane append-only after create and queue-state flows.
- Extended the entitlement backend lane again by adding sync-history removal APIs plus a dedicated management panel, then wiring `/account/access/entitlements` and `/admin/entitlements` to remove stale access-transition rows from the shared entitlement-sync store instead of leaving that lane create-only after sync-change writes.
- Extended the shared execution-ledger backend lane again by adding run-removal APIs plus a reusable management panel, then wiring `/admin/source-jobs` and `/admin/source-mapping-desk` to remove stale source-job and archive-refresh execution rows instead of leaving operator run history append-only.
- Extended the billing-event backend lane again by adding event-removal APIs plus a dedicated management panel, then wiring `/account/billing` and `/admin/billing-ledger` to remove stale payment-event rows from the shared billing store instead of leaving that lane create-only after invoice and event writes.
- Extended the broker-sync backend lane again by adding sync-run removal APIs plus a dedicated management panel, then wiring `/account/brokers` to remove stale broker queue-window rows from the shared broker-sync store instead of leaving that lane append-only after sync-run creation.
- Tightened compare-route truth again by introducing canonical stock and mutual-fund compare ordering across search intent, asset-page compare handoffs, compare-route coverage exports, and the compare pages themselves, so reversed query order now collapses into one stable demo-ready URL instead of creating duplicate left/right variants with inconsistent registry coverage.
- Extended the portfolio import backend lane again by adding import-run removal APIs plus a dedicated management panel, then wiring `/portfolio/import` to remove stale import history rows from the shared portfolio store instead of leaving that lane append-only after run creation and reconciliation flows.
- Extended the subscriber workspace backend lane again by adding alert-feed, inbox-item, and consent-item removal APIs plus dedicated management panels, then wiring `/account/alerts`, `/account/inbox`, and `/account/consents` to remove stale subscriber rows from the shared workspace store instead of leaving those surfaces append-only after save flows.
- Added a plain-English `Launch Today Checklist` directly to `/build-tracker`, backed by a shared save store plus admin save API, so non-developer launch operators can check off domain, auth, provider, billing, email, compliance, and rehearsal steps while recording their own entered details and notes in one place.
- Extended the market-history, derivatives, source-job, and archive-refresh backend lanes again by adding delete APIs plus dedicated management panels, then wiring `/admin/market-data`, `/admin/source-jobs`, and `/admin/source-mapping-desk` to remove stale backend rows from the shared stores instead of leaving those desks append-only after create, update, and run flows.
- Extended the research-archive and notification-event backend lanes again by adding removal APIs plus dedicated management panels, then wiring `/admin/research-archive`, `/account/consents`, and `/admin/delivery-layers` to remove stale archive and consent-aware delivery rows from the shared stores instead of leaving those desks append-only after create and update flows.
- Extended the subscription-lifecycle backend lane again by adding lifecycle-job and recovery-action removal APIs plus dedicated management panels, then wiring `/account/billing/lifecycle`, `/account/billing/recovery`, and `/admin/payment-events` to remove stale lifecycle and recovery rows from the shared store instead of leaving those desks append-only after create and update flows.
- Tightened search-to-screener truth again by adding a thematic `Financials` screener filter that groups banks, NBFCs, diversified financials, and insurers under one defensible route-backed lane, then wiring the screener workspace and smart-search prefill flow to honor that broader finance intent instead of collapsing those queries into only pure-bank filters.
- Extended the search-index backend lane again by adding lane-removal APIs plus a management panel, then wiring `/admin/search-screener-truth` to remove stale search-index rows from the shared store instead of leaving that desk append-only after create and update flows.
- Extended the AI persistence backend lane again by adding retrieval-dataset, generation-run, and answer-packet removal APIs plus management panels, then wiring `/admin/knowledge-ops` and `/admin/ai-ops` to remove stale AI memory rows from the shared store instead of leaving that lane append-only after create and update flows.
- Extended the workspace backend lane again by adding a consent-item save API and shared consent-write panel, then wiring `/account/consents` and `/account/workspace` to persist subscriber-facing consent posture into the shared workspace store instead of leaving consent summaries static beside delivery-channel updates.
- Extended the broker-sync backend lane again by adding broker-target and review-row removal APIs plus management controls, then wiring `/account/brokers` and `/account/brokers/review` to remove stale broker targets and review rows from the shared broker-sync memory store instead of leaving that lane append-only after create and decision writes.
- Extended the broker-sync backend lane by adding broker-target and review-row create APIs plus create panels, then wiring `/account/brokers` and `/account/brokers/review` to append brand-new connection targets and approval-first review rows into the shared broker-sync memory store instead of limiting that lane to starter targets plus sync-run creation and review-state edits.
- Extended the portfolio import backend lane by adding a review-queue create API and create panel, then wiring `/portfolio/import` to append brand-new mismatch rows into the shared portfolio memory store instead of limiting reconciliation to starter queue rows plus decision updates.
- Extended the rollback backend lane by adding a rollback-scenario create API and create panel, then wiring `/admin/rollback-center` and `/admin/revisions` to append brand-new rollback scenarios into the shared editorial memory store instead of limiting rollback coverage to the starter scenarios plus queue-state edits.
- Extended the search-index, market-history, and derivatives backend lanes by adding create APIs and create panels, then wiring `/admin/search-screener-truth` and `/admin/market-data` to append new search lanes, retained-history lanes, derivatives snapshots, and derivatives analytics rows into the shared memory stores instead of capping those desks at the original starter records.
- Extended the AI persistence lane by adding create APIs and create panels for retrieval datasets, generation runs, and answer packets, then wiring `/admin/knowledge-ops` and `/admin/ai-ops` to append new grounded AI rows into the shared memory store instead of capping those desks at the starter memory set.
- Extended the source-job and archive-refresh pipeline lanes by adding create APIs and create panels, then wiring `/admin/source-jobs` and `/admin/source-mapping-desk` to append new adapter and archive-family queue rows into the shared pipeline stores instead of limiting those desks to the starter backlog.
- Extended the research-archive backend lane by adding a record-create API and create panel, then wiring `/admin/research-archive` to append new archive rows into the shared archive memory store instead of limiting that lane to seeded starter records plus update-only edits.
- Extended the lifecycle backend lane by adding create panels for account lifecycle jobs, subscriber recovery actions, and ops lifecycle jobs, then wiring `/account/billing/lifecycle`, `/account/billing/recovery`, and `/admin/payment-events` to append new queue rows into the shared lifecycle store instead of exposing that append behavior only through update-style forms.
- Extended the billing backend lane by adding a payment-event create API and shared event-write panel, then wiring `/account/billing` and `/admin/billing-ledger` to append new billing-history rows into the shared billing-memory store instead of treating related payment events as read-only samples beside invoice rows.
- Extended the entitlement backend lane by adding a general sync-change create API and panel, then wiring `/account/access/entitlements` and `/admin/entitlements` to append new access-transition history rows and current-state updates into the shared entitlement-sync store instead of treating transition history as seeded examples plus manual overrides only.
- Extended the notification-event backend lane by adding a real delivery-event create API and shared event-write panel, then wiring `/account/consents` and `/admin/delivery-layers` to save new consent-aware delivery rows into the shared event bus instead of leaving delivery history fixed to seeded preview records.
- Extended the broker-sync backend lane by adding durable broker activity history plus a real sync-run create API and panel, then wiring `/account/brokers` to persisted run creation, registry activity counts, and recent broker actions instead of leaving sync windows as seeded preview rows only.
- Extended the workspace and portfolio backend lanes again by adding durable activity history to the shared subscriber-workspace and portfolio stores, then wiring `/account/workspace`, `/portfolio`, and `/portfolio/import` to recent persisted action trails while the import-review route now also has a first-class import-run create API and panel instead of treating import history as seeded rows only.
- Upgraded the mutual-fund compare route into a clearer allocator decision surface by adding a top-fold verdict layer, side-by-side best-fit cards, and alternate compare-route rotation, while also hardening `tsc --noEmit` against stale `.next` incremental state by disabling TypeScript incremental cache in `tsconfig.json` so local truth checks stop failing on old generated path collisions.
- Tightened stock and fund search truth again by teaching direct-intent matching to understand shorthand ticker-style aliases plus `&` and `and` naming variants across stocks, sectors, fund categories, and IPOs, while also correcting the banking screener handoff so high-ROE bank queries now land on a profitability-and-scale preset instead of relying on a misleading generic low-debt rule.
- Closed a real search-truth gap by promoting ETFs, PMS, AIF, and SIF routes into the shared smart-search and autocomplete graph, adding direct product and family intent matching, wealth-result context, family-hub follow-through, and stronger category boosts so those routes no longer disappear behind manual browsing while the canonical coverage export now points ETF rows at the correct `/etfs/[slug]` path.
- Tightened search-to-screener truth again by expanding the screener intent model to recognize broader shortlist language like blue-chip leadership plus still-pending valuation, dividend, growth, ownership, and volume lanes, then moved smart-search focus precedence so broad metric-led stock queries now lead with the screener workflow before falling back to sector hubs.
- Added a shared compare-handoff hero card and wired it into the stock and mutual-fund detail routes so the strongest peer matchup now appears above the first research modules with confidence, truth, decision-read, and direct CTA context instead of being buried deeper in the page.
- Tightened compare-route search polish again by teaching the shared ranker to boost compare and tracker categories when queries explicitly ask for head-to-head or index-style routes, while also making smart-search focus cards prioritize compare pages ahead of single-asset or hub handoffs whenever the user is clearly asking for a side-by-side decision flow.
- Tightened tracker accuracy and search truth by expanding direct-intent aliases plus ranked catalog language for Nifty 50, Sensex, Bank Nifty, and Fin Nifty, then teaching the shared search ranking layer to treat tracker, breadth, heatmap, and benchmark wording as index-style intent so common queries like `nifty bank tracker` or `financial index` land on the right route more reliably.
- Added a shared launch-evidence action store with owner, proof, next-step, and operator-note persistence, then wired it into the evidence board, signoff packet, launch-day console, and evidence export so final launch proof can be worked and recorded inside the app instead of living as read-only registry summaries.
- Added a postbuild Next-artifact normalizer that folds `... 2.json`, `... 3.ts`, and other conflict-suffixed `.next` files back into canonical paths, so `next start` and standalone `tsc` stop breaking on generated manifest and type duplicates after fresh builds.
- Added a unified `Top 100 pending fixes audit` to the protected public-launch backlog and mirrored its summary onto `/build-tracker`, so unresolved viewer-facing gaps, deeper backend work, and operator activation blockers now live in one explicit in-product backlog instead of being reconstructed from scattered desks and chat history.
- Added a shared launch-backlog action store with owner, working-state, and next-step persistence, then wired it into `/admin/public-launch-backlog`, `/admin/launch-day-console`, and the pending-audit export so the top-100 backlog now behaves like an operator lane instead of a read-only report.
- Tightened placeholder honesty on the biggest preview-backed user routes by wiring current-state and expected-state truth panels into portfolio, alerts, watchlists, screens, inbox, option-chain, trader workstation, and Market Copilot, so those pages now expose the same reality framing instead of drifting into inconsistent launch-language.
- Added a CSV export path for the new pending-audit feed and wired it into launch-readiness, build-tracker, and the public-launch backlog, so the full 100-item pending list can now be reviewed in-product, fetched as JSON, or shared as a flat export without scraping the UI.
- Disabled Webpack's persistent dev cache in `next.config.ts` after repeated `.next/dev` pack-cache corruption was knocking out route manifests and crashing localhost, so the local audit and build workflow now has a stronger default runtime posture.
- Fixed the shared local auth bypass boundary by teaching the Supabase proxy middleware to honor the same bypass flag as the rest of the app, then restarted the dev server in a non-TTY session so localhost can render `/`, `/launch-readiness`, `/build-tracker`, `/admin/public-launch-backlog`, and the new audit export cleanly again.
- Hardened launch-safety again by taking `/build-tracker`, `/launch-readiness`, and `/source-readiness` out of crawl and sitemap circulation, so ops-only decision pages no longer risk being indexed just because they live outside `/admin`.
- Expanded the launch-config source desk one step further by adding dedicated Alpha Vantage and Finnhub credential fields plus credential-readiness summaries on the launch-config, market-data, and source-mapping desks, so keyed upstream execution gaps are now visible alongside the saved source URLs.
- Deleted the dead duplicate `launch-config-console-client 2.tsx` component so the repo stops carrying an outdated shadow console that could drift from the real launch-config surface.
- Expanded the launch-config console into a stronger India-market source desk by adding explicit editable fields and saved defaults for NSE all-indices, option chain, bhavcopy, BSE quote and announcements, AMFI and MFAPI NAV sources, FX primary plus fallback, news, filings, and corporate actions, then mirrored that configured source stack onto `/admin/market-data` and `/admin/source-mapping-desk` so ops pages now show the real upstream URLs instead of leaving them buried in setup notes.
- Tightened stock and mutual-fund hub search truth by moving the shared discovery workspace off its old substring scorer and onto the same typo-tolerant, token-aware ranking layer used by broader smart search, while also exposing top-match query signals so public hub searches explain why a route ranked first instead of feeling arbitrary.
- Tightened mutual-fund search truth across both `/search` and the public fund hub by adding category-level investor-language aliases for passive/index, tax-saver, hybrid-allocation, debt, and style-box phrasing, then feeding those aliases plus manager and portfolio cues into the fund discovery workspace so more real-world allocator queries land on the right routes instead of depending on literal category labels alone.
- Migrated the project-wide brand and launch-domain defaults to `Riddra` and `https://riddra.com`, updating shared copy, metadata surfaces, launch-config defaults, package identity, and tracker language so the codebase no longer carries the old placeholder brand in launch-facing paths.
- Fixed a real pre-launch domain and indexing bug by moving metadata, auth redirects, structured SEO URLs, robots, and sitemap generation onto the runtime site URL, then stripping protected `/admin` and `/account` routes out of the sitemap and adding route-level `noindex` metadata across admin, account, and auth surfaces so a custom-domain launch does not inherit the old hardcoded fallback-host posture.
- Fixed a real launch-readiness logic bug where missing public auth activation still showed the auth foundation as `In progress` instead of `Pending`, then added an explicit `Immediate blockers before DNS cutover` section on `/launch-readiness` so the remaining Riddra.com go-live gaps are visible in one place inside the app.
- Mirrored the same DNS-cutover blocker list into `/build-tracker`, so the main project tracker now shows the exact launch-critical activation and verification lanes instead of forcing that decision to live only inside the dedicated readiness page.
- Removed the remaining internal legacy-name leftovers by renaming the legacy-tool wrapper classes, switching the health route service name to `riddra-web`, and moving market-data secret headers onto `x-riddra-refresh-secret` so the codebase now lines up on the Riddra identity end to end.
- Upgraded the ingest-and-archive backend lanes by adding a shared file-backed execution ledger, then wiring real manual run APIs and run-history panels into `/admin/source-jobs` and `/admin/source-mapping-desk` so adapter backlog and archive-refresh posture are now paired with explicit execution outcomes instead of only editable queue cards.
- Tightened search-to-screener handoff again by teaching the screener intent layer to recognize explicit screening workflow language like screener, screen, filter, shortlist, and scan, then adding a dedicated `Banking quality` saved stack plus smarter stack selection for banking high-ROE low-debt queries so search and screener flows land closer to the intended shortlist instead of dropping users into a looser sector-only browse state.
- Tightened search-to-screener truth by teaching smart search to recognize metric-heavy stock-discovery queries as screener workflows, then wiring `/search` actions to open `/screener` with prefilled free-text, sector, truth-state, compare-only, and best-fit saved-stack context so queries like banking high roe low debt or verified compare ideas no longer dump users into a generic sector page that makes them rebuild the screen manually.
- Tightened compare-route truth across the stock, mutual-fund, search, and screener flows by making the shared peer-ranking layer prefer same-cluster matchups that also carry delayed or managed route truth, while the shared compare cards now expose matchup quality explicitly so demo users can tell a high-confidence peer from a fallback proxy before opening the side-by-side route.
- Upgraded the public stock and mutual-fund hub cards into stronger demo-ready discovery surfaces by surfacing decision metrics, truth state, ownership or portfolio posture, factsheet evidence, and the top ranked compare handoff directly in the listing layer instead of forcing users to click into detail routes before understanding why a page or matchup matters.
- Hardened the mutable preview stores again by serializing file-backed writes across workspace, portfolio, broker sync, billing, entitlement sync, notifications, and lifecycle memory so concurrent backend saves stop clobbering one another during closure passes.
- Upgraded the AI persistence lane with real write-capable admin routes and action panels for retrieval datasets, generation runs, and answer packets, then tightened editorial change control with queue-safe revision and rollback writes plus richer registry visibility on the revisions desk.
- Tightened smart-search and screener truth again by adding shared compact-phrase and typo-tolerant token matching, so autocomplete, `/search`, and screener ranking now recover better from imperfect queries like compressed fund-category phrases, singular-plural drift, and light misspellings instead of depending only on exact token overlap.
- Tightened screener truth again by upgrading free-text lookup from plain keyword matching into an intent-aware ranking pass, so sector cues plus quality, leverage, scale, truth-state, and compare-workflow language now push the right route-backed rows higher and the UI explains which intents were detected instead of leaving users to guess why a name ranked first.
- Started the backend-side subscriber workspace memory push by adding a file-backed per-user workspace store and wiring the protected watchlists, alerts, saved screens, inbox, consent center, workspace hub, and account overview to that shared snapshot instead of reading only static sample arrays.
- Started the portfolio audit-trail backend push by adding a file-backed per-user portfolio memory store and wiring `/portfolio`, `/portfolio/import`, and `/portfolio/manual` to persisted import runs, reconciliation queue state, manual draft posture, and preview holdings snapshots instead of relying only on static review examples.
- Started the ingest-worker backlog push by adding a file-backed source-job memory store and wiring `/admin/source-jobs` to persisted adapter backlog, retry posture, next-run windows, and hot-cache lanes instead of leaving the worker-and-queue story as static planning copy.
- Started the archive-refresh backlog push by adding a file-backed official-source archive queue and wiring `/admin/source-mapping-desk` plus `/admin/research-archive` to persisted pending writes, document backlog, next-run windows, and continuity lanes instead of keeping archive automation purely in tracker prose.
- Started the search-index backlog push by adding a file-backed search-index memory store and wiring `/admin/search-screener-truth` to persisted route counts, alias groups, typo-protection posture, and backlog lanes instead of treating search-index backend work as only a tracker sentence.
- Started the market-history backlog push by adding a file-backed market-history memory store and wiring `/admin/market-data` to retained-series counts, verified-vs-preview continuity, refresh windows, and replay backlog lanes instead of keeping chart-history persistence as only a tracker item.
- Started the billing-ledger backlog push by adding a file-backed billing-memory store and wiring `/admin/billing-ledger`, `/admin/payment-events`, and `/account/billing` to persisted invoice rows, lifecycle posture, and payment-event continuity instead of leaving billing persistence as static preview cards.
- Started the entitlement-sync backlog push by adding a file-backed entitlement-sync memory store and wiring `/admin/entitlements` plus `/account/access/entitlements` to persisted access changes, grace-period state, and subscriber-visible entitlement continuity instead of leaving billing-to-access mapping as static audit samples.
- Started the research-archive enrichment backlog push by adding a file-backed archive-memory store and wiring `/admin/research-archive` plus the stock-route `News and filings watch` lane to persisted filings, results-memory, and route-targeted archive rows instead of leaving research continuity as descriptive copy only.
- Started the notification-event backlog push by adding a file-backed notification-event memory store and wiring `/admin/delivery-layers` plus `/account/consents` to persisted channel mapping, consent-aware suppression, retry posture, and recent delivery-event history instead of leaving messaging logic as static delivery-layer notes.
- Started the broker-sync backlog push by adding a file-backed broker-sync memory store and wiring `/account/brokers` plus `/account/brokers/review` to persisted broker targets, queue posture, sync runs, and review rows instead of leaving broker continuity as static rollout and review cards.
- Started the subscription-lifecycle backlog push by adding a file-backed lifecycle-memory store and wiring `/account/billing/lifecycle`, `/account/billing/recovery`, and `/admin/payment-events` to persisted renewal audits, failed-charge recovery jobs, grace-review posture, fallback-access cleanup, and subscriber-facing recovery actions instead of leaving recovery automation as lifecycle copy alone.
- Started the derivatives backlog push by adding a file-backed derivatives-memory store and wiring `/option-chain`, `/trader-workstation`, and `/admin/market-data` to persisted strike windows, preview chain snapshots, analytics lanes, and refresh backlog posture instead of leaving derivatives depth as static workstation layout copy.
- Started the AI retrieval-and-generation backlog push by adding a file-backed AI-memory store and wiring `/admin/ai-ops`, `/admin/knowledge-ops`, and `/market-copilot` to persisted retrieval datasets, generation-run history, and reusable answer packets instead of leaving the copilot backend lane as planning prose only.
- Started the CMS revision-and-rollback backlog push by adding a file-backed editorial-revision memory store and wiring `/admin/revisions`, `/admin/rollback-center`, and `/admin/block-editor` to persisted revision rows, changed-field tracking, rollback scenarios, and family-level coverage posture instead of leaving change control as disconnected planning cards.
- Extended the CMS revision-and-rollback backlog push by wiring `/admin/editorial-workflows` into the same persisted revision lane and adding an exportable `/api/editorial-revision-registry` surface, so workflow review and audit export now sit on the same backend memory instead of remaining a separate static queue.
- Extended the billing-ledger backlog push by adding an exportable `/api/billing-ledger-registry` surface and wiring `/admin/billing-ledger` to shared registry counts plus CSV download, so revenue audit export now sits beside the persisted invoice and event memory instead of staying trapped inside one page.
- Extended the entitlement-sync backlog push by adding an exportable `/api/entitlement-sync-registry` surface and wiring `/admin/entitlements` to shared registry counts plus CSV download, so access-sync audit export now sits beside the persisted billing-to-access memory instead of staying trapped inside one page.
- Extended the market-history backlog push by adding an exportable `/api/market-history-registry` surface and wiring `/admin/market-data` to shared registry counts plus CSV download, so chart-history audit export now sits beside the persisted series and replay-backlog memory instead of staying trapped inside one page.
- Extended the workspace-memory backlog push by adding an exportable `/api/subscriber-workspace-registry` surface, wiring `/account/workspace` to shared registry counts plus CSV download, and adding a dedicated `Top 10 Codex Backend Lanes` section to `/build-tracker` so the active backend backlog is easier to find at a glance.
- Extended the portfolio-memory backlog push by adding an exportable `/api/portfolio-registry` surface and wiring `/portfolio` to shared registry counts plus CSV download, so import runs, review rows, holdings snapshots, and the manual draft now sit on one backend audit lane instead of separate preview pages.
- Extended the AI-memory backlog push by adding an exportable `/api/ai-generation-registry` surface and wiring `/admin/ai-ops` to shared registry counts plus CSV download, so retrieval datasets, stored runs, and answer packets now sit on one backend audit lane instead of only inside page-local memory cards.
- Extended the broker-sync and research-archive backlog pushes by adding exportable `/api/broker-sync-registry` and `/api/research-archive-registry` surfaces, then wiring `/account/brokers` and `/admin/research-archive` to shared registry counts plus CSV download so those two lanes now behave like the stronger backend audit slices instead of isolated preview pages.
- Extended the workspace and portfolio backend lanes again by adding first write APIs for watchlist creation, saved-screen creation, manual-draft saves, and portfolio review decisions, then wiring `/account/workspace`, `/portfolio/manual`, and `/portfolio/import` so those actions now write into the file-backed stores instead of only changing local component state.
- Extended the broker backend lane again by adding a first write API for broker review decisions and wiring `/account/brokers/review` so approval-state changes now write into the file-backed broker queue instead of only changing local component state.
- Extended the billing-ledger and entitlement-sync backend lanes again by adding first write APIs for preview invoice saves and manual entitlement overrides, then wiring `/account/billing`, `/admin/billing-ledger`, `/account/access/entitlements`, and `/admin/entitlements` so those actions now write into the shared file-backed stores instead of staying as read-only audit pages.
- Extended the ingest, archive, and CMS-control backend lanes again by adding first write APIs for source-job queue updates, archive-refresh posture updates, revision-log entries, and rollback queue-state changes, then wiring `/admin/source-jobs`, `/admin/source-mapping-desk`, `/admin/revisions`, and `/admin/rollback-center` so those routes now write into their shared file-backed stores instead of staying as read-only planning desks.
- Extended the search-index and market-history backend lanes by adding first write APIs for lane updates, then wiring `/admin/search-screener-truth` and `/admin/market-data` to operator update panels so search truth and chart-history persistence now support saved backend posture changes instead of staying as read-only audit cards.
- Extended the workspace, portfolio, and billing backend lanes again by adding remove/reset/archive APIs for watchlists, saved screens, manual drafts, portfolio review rows, and preview invoices, then wiring the account surfaces so those lanes now support basic state management instead of behaving like append-only preview stores.
- Extended the notification and lifecycle backend lanes by adding channel-update APIs plus action panels for consent-aware delivery mapping, and job/recovery-action APIs plus update panels across billing lifecycle, billing recovery, and payment-events so those queues are no longer read-only preview memory.
- Extended the source-job, archive-refresh, search-index, and market-history backend lanes again by serializing their persisted writes, so concurrent operator saves stop clobbering queue posture and audit state across the stronger admin desks.
- Extended the research-archive and derivatives backend lanes by adding first write APIs and update panels for archive records, derivatives snapshots, and derivatives analytics lanes, then added an exportable `/api/derivatives-registry` surface plus shared registry counts on `/admin/market-data` so those backend slices now behave like stronger auditable lanes instead of read-only preview memory.
- Extended the workspace backend lane again by adding first write APIs and action panels for alert-preference saves, alert-feed saves, and inbox-item saves, then wiring `/account/alerts` and `/account/inbox` so those subscriber signal and action queues no longer stay read-only preview memory.
- Deepened smart-search and autocomplete truth for the demo-ready stock and mutual-fund routes by expanding the shared search catalog with stock fundamentals, ownership labels, fund returns, holdings, allocation, manager, and factsheet evidence terms, while also widening ranking aliases and compare-intent cues so natural queries like ownership, holdings, overlap, against, better, or ticker-vs-peer requests land on the right detail or compare pages more reliably.
- Added a dedicated `Backend Still Pending` checklist to the build tracker so the team can separate true backend backlog from live-activation work; it now groups the remaining gaps into data and pipeline backend, subscriber persistence, revenue and access backend, and research or CMS or intelligence backend instead of mixing those items into generic launch talk.
- Reworked the public stock and mutual-fund detail routes so their demo-critical research blocks now derive from real route data, compare candidates, ownership posture, return tables, portfolio concentration, factsheet evidence, and verification state instead of generic placeholder prose, making both page families read more like decision surfaces before live feeds are fully activated.
- Marked Phase 17 build work complete after tightening the stock-chart truth path so source-entry OHLCV now renders as an honest source-entry chart state instead of being labeled like verified provider bars, while the live provider credential and licensed-source steps are now treated as external activation handoff rather than missing build plumbing.
- Marked Phase 18 complete after locking the first-wave coverage baseline around a 100-plus stock graph, a 16-fund bench, broader wealth and IPO families, route-backed search and screener truth surfaces, and deeper learning plus webinar child-route depth instead of leaving those wider first-wave upgrades trapped in partial status.
- Marked Phase 19 build work complete after adding a dedicated subscriber activation packet with CSV export plus a linked admin desk, so auth, entitlements, billing, webhook truth, support delivery, workspace continuity, and conversion-path verification now roll up into one portable handoff instead of staying scattered across readiness pages.
- Marked Phase 20 build work complete after adding a dedicated launch rehearsal packet with CSV export plus a linked admin desk, so mobile, smoke, chart, placeholder, reliability, announcement, and go-no-go proof now roll up into one portable handoff instead of staying fragmented across launch-QA boards.
- Expanded the public stock graph past the Top 100 first-wave threshold and widened the mutual-fund bench to 16 routed funds across equity, hybrid, and debt categories, so search, compare, sitemap, and category discovery now inherit a materially broader Phase 18 universe than the earlier showcase set.
- Expanded the source-entry console into a stronger Phase 17 operator desk by seeding native delayed OHLCV coverage across the full first trusted stock set, adding a new mutual-fund factsheet-evidence lane, and surfacing that document workflow on public fund detail plus compare routes instead of leaving it as tracker-only prose.
- Wired the combined launch-evidence packet into `/admin/launch-signoff-packet` and `/admin/launch-evidence-board`, so the final owner brief and final proof board now reflect real Phase 20 mobile, smoke, chart, placeholder, reliability, runbook, and announcement lanes instead of older summary-only signals.
- Added a combined launch-evidence registry with CSV export, then surfaced it inside `/admin/public-launch-qa`, `/admin/go-no-go`, and `/admin/release-gate-board` so mobile, smoke, chart, placeholder, reliability, launch-day, and announcement proof now compress into one portable final-call packet instead of staying split across multiple Phase 20 desks.
- Added an exportable launch-day runbook registry that combines same-day checkpoints, the live execution queue, and fallback actions, then surfaced that lane inside both `/admin/launch-day-runbook` and the broader Phase 20 public-launch QA desk so release-day sequencing is now auditable outside page-only summaries.
- Added a route-level mobile QA registry with CSV export, upgraded `/admin/mobile-qa-matrix` to use direct route checkpoints instead of only area summaries, and folded that mobile review lane into the broader Phase 20 public-launch QA registry so compact-screen launch work is now auditable like the rest of launch safety.
- Added a combined reliability registry with CSV export across reliability controls, incident response lanes, recovery readiness, and rollback scenarios, then surfaced that lane inside both `/admin/reliability-ops` and the broader Phase 20 public-launch QA desk so incident-drill posture is no longer trapped in static admin copy.
- Added a dedicated live smoke-test journey registry with CSV export, rebuilt the `/admin/live-smoke-tests` desk around explicit public, auth, billing, support, and operator route sequences, and surfaced that same smoke lane inside public-launch QA, release checks, and preflight so Phase 20 no longer treats smoke testing as vague tracker prose.
- Reworked screener free-text lookup into a ranked route-aware search pass, so exact ticker, company-name, sector, truth-state, tag, and compare-intent queries now surface the strongest stock first instead of relying on plain substring filtering, and the workspace now explains why the top match won.
- Enriched the shared compare-route cards with delta-based decision reads across stock, mutual-fund, and compare flows, so demo users can now see exactly how each peer differs on profitability, leverage, cost, scale, or overlap before opening the side-by-side route.
- Fixed the recurring verification trap in local automation by forcing production builds to start from a clean `.next` directory, so stale duplicate build artifacts no longer break `next build` with malformed generated-type paths.
- Replaced the dead Next 16 `next lint` script with a real TypeScript verification pass and tightened `tsconfig.json` to depend only on stable generated route types, so repo checks now run cleanly without requiring leftover dev-session files.
- Normalized smart-search direct-intent and compare-intent suggestions through the shared presentation layer, so autocomplete and the `/api/search/suggestions` route now return one consistent truth-shaped payload instead of mixing raw catalog entries with rendered suggestions.
- Changed the screener’s first-load state to start from an all-stock route-backed view instead of a prefiltered saved stack, and tightened the filter copy around compare-only mode plus no-filter posture so the truth-first demo story is clearer on first visit.
- Polished compare-route actions across stock, mutual-fund, and compare pages so the CTA now spells out the exact matchup being opened and the secondary action reads as a direct detail-page handoff instead of generic compare wording.
- Tightened the screener lookup layer so in-screen search now indexes stock symbols alongside route state, truth detail, and compare context, and surfaced ticker labels directly in result cards so demo users can find obvious names with ticker-style queries instead of relying only on company-name matches.
- Tightened the premium-preview truth layer again by removing fake strike rows from the option-chain route, reframing trader-workstation summary cards around real verified anchors instead of inflated live-depth counts, and turning Market Copilot into an explicitly guided-preview surface with a stronger truth notice instead of implying live AI continuity.
- Switched the four flagship index pages onto the free native TradingView chart library with local timeline and breadth overlays, so `/nifty50`, `/banknifty`, `/finnifty`, and `/sensex` no longer depend on the hosted widget path that was causing the worst popup and blank-load behavior.
- Moved the shared homepage and `/markets` index-card chart layer onto the free native TradingView chart library too, so the front door and market overview now depend on hosted widgets only where stock-specific chart embeds are still genuinely needed.
- Added a stock delayed-close lane to the source-entry console and wired completed stock routes to prefer those backend-saved rows over hardcoded manual overrides, so Phase 17 quote cleanup can now happen visually from admin instead of through code edits.
- Added a stock OHLCV lane to the source-entry console, wired the chart snapshot resolver to read persisted OHLCV rows, seeded the backend store with a Tata Motors test series, and let completed stock routes promote into native candlestick charts whenever verified or source-entry OHLCV exists, so Phase 17 and Phase 20 now reduce stock-chart trust risk without waiting on code edits for each symbol.
- Consolidated completed-stock delayed-close fallback into the source-entry backend path instead of split hardcoded overrides, so the same operator console now controls both manual delayed closes and native OHLCV promotion for the first trusted stock rollout.
- Added native stock-chart support to the shared market-chart grid and moved the homepage Tata Motors tile onto the persisted OHLCV path, so the front-door chart strip no longer depends on a hosted stock widget and Phase 20 chart risk is narrower again.
- Turned the tools hub into a real interaction surface by adding an inline calculator explorer plus a working breakout-checklist panel, so the strongest public tools no longer read like a pure catalog before users open the detail routes.
- Added real lesson-level course routes under `/courses/[slug]/lessons/[lesson]`, wired the course detail pages into those lesson destinations, and exported them through the sitemap, so the education layer now has actual lesson-depth routes instead of stopping at course-overview shells.
- Added real persona-track routes under `/learn/tracks/[slug]`, wired the learn hub into those destinations, and gave each track concrete route handoffs into courses, webinars, reports, and research surfaces, so the learning layer no longer stops at three overview cards.
- Added real event-archive pages under `/learn/events/[slug]`, linked them from both the learn hub and `/reports/results-calendar`, and folded those routes into search plus sitemap discovery, so results, IPO, and NAV archive continuity now has public route depth instead of only admin/archive framing.
- Widened the demo refresh rehearsal so `/api/market-data/refresh` now writes the full first trusted stock quote set plus the first trusted fund NAV set instead of only a tiny partial demo, and synced the market-data, handoff, external-activation, and launch-readiness surfaces to describe that broader Phase 17 path more honestly.
- Added real public child routes under `/mentorship/[slug]` and `/community/[slug]`, wired the mentorship and community hubs into those destinations, and folded the new guided-program routes into search plus sitemap discovery so Phase 18 learning depth no longer stops at overview-only shells.
- Added real webinar registration and replay child routes under `/webinars/[slug]/register` and `/webinars/[slug]/replay`, wired webinar discovery into those destinations, and folded the new event-depth routes into search plus sitemap discovery so Phase 18 education depth no longer stops at overview and detail pages alone.
- Added protected subscriber routes under `/account/access/entitlements` and `/account/billing/recovery`, wired the account, workspace, access, and billing surfaces into those destinations, and extended the subscriber-launch registry plus Phase 19 tracker language so entitlement review and billing-recovery posture no longer stay trapped inside summary cards.
- Added a protected `/account/support` route with runtime contact channels, recovery handoffs, and subscriber-safe help rules, then wired it into the account hub, workspace hub, public help route, sitemap, and Phase 19 readiness plus registry surfaces so support posture no longer lives only in admin pages or scattered public copy.
- Added a protected `/account/billing/lifecycle` route so activation, renewal, failed-charge, and fallback posture can be reviewed from the subscriber side, then wired it into billing, workspace, account, support, sitemap, and Phase 19 readiness plus registry surfaces so subscription-state truth is easier to audit without relying on admin pages alone.
- Tightened the public alerts route into an explicitly preview-backed discovery surface instead of a semi-live-looking feed, and synced the placeholder-honesty plus subscriber-truth layers so alert delivery risk is tracked more honestly across launch QA and workspace readiness.
- Widened the first trusted stock-chart rollout from a Tata-only audit lane into the full completed stock set across market-data targets, chart verification, provider onboarding, the market-data playbook, and source-entry console messaging, so Phase 17 now tracks stock-chart trust as a set instead of a one-symbol exception.
- Tightened the broker connections and broker review routes into explicitly preview-backed workflow surfaces, added a dedicated placeholder-honesty row for broker-review decisions, and synced the Phase 19 plus 20 tracker language so broker continuity stops looking more live than it really is.
- Tightened watchlist and saved-screen cards into explicit preview examples, added dedicated placeholder-honesty rows for both subscriber-memory surfaces, and synced Phase 19 plus 20 tracker language so saved-workspace state stops reading as durable before per-user persistence is real.
- Re-based Phases 17 to 20 more conservatively after a route-by-route launch audit, added explicit blocker steps for provider credentials, NSE chart stabilization, Top 100 coverage expansion, real auth and billing activation, and end-to-end smoke validation, and downgraded the active-phase percentages so the tracker now reflects product reality more than admin-surface completeness.
- Added an import-first implementation checklist directly inside the build tracker, covering which libraries and services should be adopted now, activated next, kept and expanded, or evaluated later so platform acceleration decisions stop getting lost in chat history.
- Wired the Phase 17 market-data execution path to the saved launch-config console values instead of env-only state, so provider sync, signed ingest, demo refresh, Supabase admin writes, launch readiness, and release-check surfaces now honor the actual admin setup flow before real credentials are entered.
- Added a mutual-fund NAV lane to the source-entry console and wired the fund hub, fund detail, and fund compare routes to prefer source-entry delayed NAV rows before falling back to generic waiting-feed states, so tracked fund pages can now be corrected from admin while the AMFI-backed pipeline is still being activated.
- Extended the verified market-data ingestion path, provider sample payload, and first-rollout target tracking to include a first trusted mutual-fund NAV set, so Phase 17 fund activation is no longer stuck on admin-only source-entry fallback and now has a real provider-backed execution path ready for legitimate delayed NAV payloads.
- Expanded the seeded mutual-fund bench from a tiny showcase set into a broader category mix with flexi-cap, small-cap, index, ELSS, and balanced-advantage routes, so fund categories, compare recommendations, search suggestions, and sitemap-backed discovery now inherit a more credible public fund graph.
- Expanded the wealth-product bench across ETFs, PMS, AIF, and SIF, and upgraded the public wealth family hubs with ticket-posture, status-mix, category, benchmark, and compare-lane summaries, so Phase 18 now exposes broader wealth coverage on the public product instead of hiding it only in the registry layer.
- Expanded the IPO archive from a three-route showcase into a broader mainboard-plus-SME bench, so the IPO hub, SME hub, search discovery, sitemap coverage, and canonical route graph now inherit more believable event-led breadth instead of depending on only a tiny issue set.
- Expanded the visual launch-config console into a broader backend setup desk covering auth, provider endpoints, reference-data URLs, charting, communications, analytics, AI, billing, and automation or storage hooks, so future platform credentials no longer need to be scattered across code and ad-hoc notes.
- Added an exportable provider-onboarding registry plus admin summary cards for configuration, execution entry points, and first trusted rollout targets, so Phase 17 now has a portable audit surface instead of only checklist copy.
- Added an exportable subscriber-launch registry plus admin summary cards for billing, support, plan truth, and route checkpoints across pricing, signup, access, billing, and gated workspace paths, so Phase 19 now has a portable audit surface instead of only checklist copy.
- Added a combined public-launch QA registry plus admin summary cards for the broad QA checklist and mobile matrix together, so Phase 20 now has one portable audit surface instead of splitting launch safety across separate pages.
- Added a source-mapping registry plus admin summary cards for intake families, owners, source classes, current route coverage, and first-wave targets, so Phase 18 now has a portable human-handoff audit surface instead of leaving source planning only in page copy.
- Added a market-data target registry plus admin summary cards for Tata Motors quotes, Tata Motors charts, and the tracked index rollout set, so Phase 17 now has a portable rollout-truth surface instead of only card-level target status.
- Added a source-job registry plus admin summary cards for adapter planning, sample-payload validation, verified ingest, provider sync, and refresh control, so Phase 17 now has a portable ingest-ops audit surface instead of only a planning page.
- Added a payment-readiness registry plus admin summary cards for billing activation checks, webhook coverage, billing workspace notes, and plan-to-feature mapping, so Phase 19 now has a portable billing-truth surface instead of only checklist copy.
- Added a communication-readiness registry plus admin summary cards for support contact, transactional delivery, alerts, billing communication, and launch-day feedback intake, so Phase 19 now has a portable communication-truth surface instead of only checklist copy.
- Added a launch-decision registry plus go/no-go summary cards for approvals, decision blockers, launch scorecard checks, activation steps, and live smoke tests, so Phase 20 now has a portable final-call audit surface instead of relying only on the go/no-go page itself.
- Added a screener-metric registry plus admin summary cards for route-backed metrics, decision-handoff fields, and blocked future factor lanes, so Phase 18 now has a portable metric-truth audit surface instead of describing screener depth only in copy.
- Added a trust-signoff registry plus admin summary cards for legal ownership, support promises, domain posture, risk disclosure, and external incident confidence, so Phase 19 now has a portable trust-audit surface instead of leaving broad-launch signoff only on a static review page.
- Added a reference-parity registry plus admin summary cards for charts, stocks, funds, IPOs, wealth, screeners, and learning, so the remaining product-depth gaps can now be exported and reviewed as one portable Phase 18 audit surface instead of staying trapped inside one static page.
- Added a provider-fallback registry plus admin summary cards for fallback profiles and switchboard checkpoints across auth, billing, communications, AI, brokers, and storage, so Phase 17 now has a portable resilience audit surface instead of leaving outage and rollback posture as page-only notes.
- Added a provider-config registry plus admin summary cards for provider profiles, runtime checkpoints, and switchable-contract rules, so Phase 17 now has a portable provider-setup audit surface instead of leaving that lane trapped inside one static planning page.
- Added a conversion-path registry plus admin summary cards for discovery, auth, support, billing, and launch-mode checkpoints, so Phase 19 now has a portable audit of the real public-to-paid journey instead of keeping that verification flow trapped in one checklist page.
- Added a launch-approval registry plus admin summary cards for owner approvals and urgency checkpoints, so Phase 20 now has a portable owner-signoff surface instead of leaving final launch accountability inside one page-only board.
- Added an announcement-readiness registry plus admin summary cards for checklist items, launch assets, and audience angles, so Phase 20 now has a portable messaging-rollout surface instead of leaving public launch narrative planning trapped inside one admin page.
- Added a support-ops registry plus admin summary cards for help content, support routes, launch-day triage, and operating rules, so Phase 19 now has a portable support-hardening audit surface instead of leaving that lane trapped inside one static admin page.
- Added a preflight-checklist registry plus admin summary cards for checklist groups, route checkpoints, and final launch rules, so Phase 20 now has a portable last-pass audit surface instead of leaving the final preflight lane trapped inside one admin page.
- Tightened the execution tracker to call out the live blockers more honestly: broken chart surfaces, manual last-close equity data, and shell-heavy route families are now explicitly represented inside the active phase summaries instead of being left as implicit risk.
- Tightened the tracker again after a deeper audit: search and screener are now acknowledged as stronger completion anchors, while Phase 19 is tracked more conservatively because auth still leans on a local admin bypass and billing still reads as blocked rather than launch-live.
- Expanded the parity and launch tracker again after another route audit: portfolio, watchlists, alerts, tools, AI copilot, option chain, and trader workstation gaps are now explicitly represented as pending depth work instead of being hidden behind polished shell pages.
- Folded another deeper audit into the active phases too: local-admin-bypass auth, seeded invoice history, demo portfolio and alert channels, tools-hub interaction depth, market-copilot architecture-only status, learning-route depth, and the TradingView blank-void plus popup issue are now all explicitly tracked as smaller pending blockers instead of being implied risk.
- Upgraded the shared TradingView embed with a branded loading skeleton and a calmer widget configuration, so chart-backed routes no longer open with the same dead dark void while the iframe initializes.
- Tightened the tracker and launch surfaces again after a broader route audit: the system-status page now reflects market-data and transactional-delivery readiness too, launch control now spells out the exact remaining human activations more explicitly, Sensex is treated as the clean chart control case while NSE index symbol normalization stays open, and courses plus webinars are now tracked as real depth gaps instead of getting lumped into generic learning progress.
- Added per-index TradingView symbol overrides to the visual launch-config console and wired the chart-mapping layer to read them at runtime, so Phase 17 can now normalize Nifty 50, Bank Nifty, Fin Nifty, and Sensex chart symbols from admin instead of hardcoding every symbol guess in code.
- Added a dedicated index-chart symbol audit surface to Market Data Ops and the Market Data Tester, so Phase 17 now exposes current symbols, override source, candidate mappings, and route targets for Nifty 50, Bank Nifty, Fin Nifty, and Sensex from admin instead of leaving NSE chart verification as a blind manual chase.
- Standardized the free TradingView widget path more honestly by wiring the homepage and markets chart grids through the shared symbol resolver, adding a dedicated free-chart section on `/markets`, and tightening admin charting copy so the hosted widget path is clearly separated from any later self-hosted Advanced Charts or custom-datafeed work.
- Tightened Phase 19 subscriber truth by adding reusable preview-vs-verified notices across billing, watchlists, alerts, portfolio, trader workstation, and option chain, restoring signed-in protection on the account billing and alert routes, and separating real invoice truth from preview-only billing examples so subscriber pages stop looking more live than they really are.
- Extended that Phase 19 truth cleanup across the account hub, workspace hub, saved screens, broker connections, broker sync review, and access-model pages too, so the protected workspace now speaks with one consistent preview-vs-verified language instead of mixing polished structure with silent demo assumptions.
- Extended that same Phase 19 cleanup to the remaining subscriber workspace routes, so the inbox, portfolio hub, portfolio import review, and manual portfolio builder now require signed-in access and carry the same preview-vs-verified honesty instead of behaving like semi-public demo flows.
- Extended the same Phase 19 cleanup into the account setup and consent-center layer too, so those protected subscriber routes now clearly describe themselves as preview-backed setup and preference flows instead of silently implying durable onboarding and consent persistence that still is not live end to end.
- Pushed Phase 20 forward by adding a dedicated placeholder-honesty registry plus CSV export, then surfaced it inside Public Launch QA and Preflight Checklist so fake-looking billing, portfolio, option-chain, workstation, AI, and learning states are reviewed as one explicit launch-risk lane instead of scattered notes.
- Pushed Phase 20 forward again by adding a dedicated chart-verification registry plus CSV export, then surfaced it inside Public Launch QA, Release Checks, and Preflight Checklist so homepage, markets, index, stock-chart, and fund-proxy chart risk is reviewed as one explicit launch lane instead of staying buried inside general QA text.
- Upgraded the shared compare-route cards on stock, mutual-fund, and compare pages to expose candidate truth state plus key matchup metrics inline, so demo handoffs now show route credibility and peer stats before the user even opens the side-by-side route.
- Upgraded the courses and webinars layer with structured lesson plans, prerequisites, deliverables, event logistics, session assets, and companion route handoffs, so Phase 18 now has more real public education depth instead of leaving those routes as curriculum-only shells.
- Re-ranked the shared stock and mutual-fund compare-routing layer with stronger sector/category penalties, scale similarity, and benchmark-risk checks, and added explicit matchup rationale copy to compare cards, screener handoffs, and smart-search actions so demo compare suggestions now stay much closer to real peers instead of surfacing weak cross-market proxies.
- Strengthened the stock compare route with ownership and participation posture plus side-by-side fundamental blocks, so the stock matchup flow now carries promoter, institutional, public-float, and research-depth context instead of stopping at quote and ratio headline cards.
- Fixed the mutual-fund compare route’s missing 3Y visual logic by resolving CAGR-labelled return rows correctly, and added shared portfolio-overlap plus concentration posture blocks across the fund detail and compare pages so allocator demos now show actual holdings and sector differentiation instead of only headline return and cost cards.
- Reworked the public stocks and mutual-funds hubs to use the shared ranked compare-routing layer for showcase cards and card-level next steps, so demo discovery now stays aligned with the same stock/fund matchup logic already used by detail pages, search, and screener flows.
- Upgraded the screener into a stronger route-handoff surface with route-state counts, richer truth detail, chart and compare next steps, and cleaner saved-screen access, while also expanding the admin search/screener truth desk with coverage breakdowns for asset, compare, workflow, and route-backed screener inventory.
- Added explicit snapshot-truth badges to the mutual-fund detail route plus both stock and mutual-fund compare route families, so demo-ready research pages now keep source, freshness, and delayed-vs-seeded status visible even after the user moves into side-by-side decision flows.
- Upgraded smart-search intent handling so sector, fund-category, and index queries now resolve into direct route matches with focus cards and decision-handoff actions instead of falling back to only the generic ranked result stack.
- Added a protected source-entry console so index roster cleanup, metals history correction, and launch-critical public-route overrides now have a visible operator handoff inside admin instead of living only in code and chat.
- Upgraded the source-entry console from a static handoff page into a working admin entry surface with saveable forms and recent-entry lists for index rows, metals history, and verified route overrides.
- Added native Riddra history preview panels to the gold and silver tool pages so backend-saved commodity rows, source labels, and recency are visible even outside the embedded legacy widget.
- Added an exportable canonical coverage registry across stocks, funds, IPOs, ETFs, PMS, AIF, and SIF routes, and surfaced it inside the Phase 18 admin desks so route coverage and truth state can now be audited from one downloadable CSV instead of only summary copy.
- Switched sitemap asset-route generation onto the shared canonical coverage registry, so stock, fund, IPO, ETF, PMS, AIF, and SIF discovery now follows the broader live route graph instead of only the older sample arrays.
- Added an exportable compare-route registry for ranked stock and mutual-fund matchups, and wired those compare routes into the sitemap so discovery no longer depends on only one hardcoded stock pair and one hardcoded fund pair.
- Added an exportable release-check registry for the route-by-route launch matrix, so Phase 20 QA can now be reviewed and shared as one downloadable route audit instead of only inside the admin page.
- Added a protected launch-config console so Supabase, provider, billing, support, and admin setup values can now be entered visually from admin and stored on the backend instead of asking for manual code or env-file edits.
- Linked the new launch-config console into launch-control, provider-onboarding, admin home, and the active Phase 17 tracker language so visual setup now counts as a real execution unblocker instead of a side utility.
- Added a shared runtime launch-config reader so provider onboarding, subscriber readiness, public launch QA, and system status can now reflect values saved in the launch-config console instead of only raw environment variables.
- Added an exportable search-index registry and surfaced it in the Phase 18 search-truth desk, so the actual indexed route graph can now be downloaded and audited instead of being judged only from public search behavior.
- Added real Pro-tier gating across the trader-workstation layer with an account-access upgrade handoff, so premium routes now behave more like actual subscriber surfaces instead of staying fully open in generous beta mode.
- Rebuilt the release-check surface into a real route-by-route launch matrix across public discovery, research, subscriber, and operator journeys, so Phase 20 QA now anchors to actual product paths instead of generic checklist language.
- Strengthened the index breadth demo layer by reframing the rhythm card as a last-session snapshot, making the breadth strip more visible, and linking constituent rows directly into their stock pages.
- Corrected the seeded Nifty 50 roster back to 50 visible components so the public index page no longer carries the obvious 51-name inconsistency.
- Migrated the attached MasterGadgets tool bundle into the live Riddra tool system by hosting the legacy JS/CSS assets, wiring them into `/tools/[slug]`, and expanding the tool catalog with FIRE, gold, silver, PDF, and reel-downloader utilities.
- Added PHP-compat route shims for the legacy gold and silver trackers so their browser-side history-save calls no longer fail inside the Next.js app shell.
- Added live gold and silver market cards to `/markets` through a new commodity-price API layer, so metals now show up on the market overview instead of living only inside tool routes.
- Fixed the compact vertical menu behavior by closing the active details group after link selection, so mobile-style navigation no longer stays expanded after a route tap.
- Added a shared execution-phase summary layer for Phases 17 to 20 and surfaced it on both homepage and build tracker, so live data, coverage scale, subscriber truth, and launch QA are easier to review from the main product surfaces instead of only inside admin desks.
- Added a reusable winner-led matchup strip across the stock and mutual-fund compare routes, giving the strongest demo pages a second compact visual layer that scans cleanly in screenshots and live walkthroughs.
- Closed the remaining Phase 21 demo-presentation gaps by marking the visual data layer and screenshot-safe showcase routes complete in the readiness tracker, so the admin state now matches the actual route polish.
- Added a reusable demo-opening sequence layer across homepage, stocks hub, mutual-fund hub, and the strongest stock/fund detail routes so tomorrow's walkthrough path is visible before anyone starts hunting through navigation.
- Added direct asset-intent shortcuts to smart search and autocomplete, so ticker/company queries now jump to the matched stock, fund, IPO, or stock-chart route before the broader ranked catalog takes over.
- Upgraded `/search` from a flat result list into a grouped route-handoff surface with direct-match focus cards plus chart, compare, sector, category, and workflow next moves, so demo search now behaves more like a decision navigator than a ranked link dump.
- Reworked screener saved stacks and metric groups around only the metrics Riddra can currently support from route-backed records, expanded the table to the full seeded stock universe, and added cleaner empty-state handling so the discovery flow is more truthful in demos.
- Search and autocomplete now detect direct compare intent across seeded stock and mutual-fund routes, so queries that name both assets can jump into the right side-by-side page instead of depending only on the limited prebuilt compare catalog.
- Upgraded stock and mutual-fund detail pages with compare shortlists instead of one-off compare links, so demo viewers can move directly into the strongest side-by-side routes from each asset page.
- Added alternate matchup rotation on stock and mutual-fund compare routes, so compare flows now behave more like reusable decision workspaces instead of single dead-end pairings.
- Tightened screener truth by replacing mislabeled placeholder columns with honest route-backed market-cap and day-move fields, and added direct compare-route handoffs so demo workflows now stay consistent with the canonical stock pages already in the app.
- Replaced alphabetical compare pairing with a shared relevance-ranked routing layer across stock pages, mutual-fund pages, screener handoffs, and search compare entries, so the strongest demo matchups now stay closer to sector, category, scale, and performance reality.
- Added a protected launch-day console that aggregates provider activation, subscriber truth, conversion confidence, trust signoff, launch QA, and cross-lane commitments into one operator view so Phase 22 can be run from a single command center.
- Upgraded launch approvals and go / no-go so the final launch call is now tied more directly to blocked lanes, owner accountability, and the shared launch-day console instead of isolated summary pages.
- Upgraded the launch-day runbook into a more practical execution queue by tying activation steps and smoke tests together, so Phase 22 now behaves more like a real launch operator layer than a static checklist.
- Added an external activation board that pulls credentials, callback setup, communication readiness, and launch commitments into one compact handoff surface so the remaining non-code work is easier to execute.
- Added a launch posture board that consolidates launch mode, public scope boundaries, and recommendation blockers so the release stance is easier to review from one place.
- Added a release gate board that consolidates scorecard, execution queue, posture blockers, and preflight discipline into one final release view for the last Phase 22 decision layer.
- Added a launch blocker ledger that consolidates unresolved items across control, commitments, decision logic, and approvals so the remaining Phase 22 blockers are easier to assign and clear.
- Added a launch owner inbox that groups the remaining blockers and pending approvals by owner so the last Phase 22 handoffs are easier to execute under pressure.
- Added a launch signoff packet that compresses blockers, owner lanes, commitments, and top actions into one concise final Phase 22 launch brief.
- Added a launch evidence board that compresses decision mode, approvals, blocked checks, and top launch actions into one final proof layer before a public call.
- Tightened login, signup, auth-status, and account messaging so launch-facing access flows sound calmer and more trustworthy instead of leaking build-mode language.
- Cleaned contact, privacy, terms, and 404 wording so support/legal surfaces and fallback routes feel steadier and less placeholder-heavy during launch prep.
- Tightened pricing, launch-readiness, and alerts wording so conversion and retention pages sound calmer and less beta-note heavy while staying honest about the current launch posture.
- Cleaned shared pricing metadata and source-readiness wording so public plan language and readiness pages stop reintroducing old build-mode or coming-soon phrasing.
- Softened raw pending-state labels on stock and mutual-fund pages so feed readiness stays honest without sounding mechanical in demos or public walkthroughs.
- Tightened screener truth messaging, chart waiting-state wording, and smart-search prompt copy so research and discovery routes feel more launch-ready without overstating live data depth.
- Smoothed the remaining feed-state wording on stock, mutual-fund, and screener surfaces so the core research routes read more deliberately during demos and early public traffic.
- Cleaned homepage, onboarding, and index-entry wording so the main front-door routes sound more product-ready and less like internal launch narration.
- Smoothed search, methodology, and stock-chart wording so trust and chart routes sound less operational and more like a polished public product.
- Cleaned results-calendar, markets, market-copilot, and learn wording so the discovery and education layer sounds more user-facing and less like product-strategy narration.
- Added an external prerequisites handoff page and completed Phase 22 from the build side, so the remaining auth, payment, support, and approval tasks are now explicitly tracked as off-repo activation work instead of missing product surfaces.
- Reworked smart search and autocomplete to use a shared local ranking layer with token coverage, phrase boosts, and intent aliases, so natural queries now surface relevant stocks, charts, IPOs, funds, tools, and learning routes more reliably than the old exact-substring matcher.
- Expanded the shared search catalog so autocomplete and smart search now include compare routes, sector/category hubs, workflow entry points, and learning surfaces instead of stopping at only basic asset pages.
- Added a protected market-data tester so operators can load the Tata Motors plus indices sample payload, validate provider JSON, and catch shape issues before touching verified ingestion.
- Added reusable market-data status badges across the Tata Motors stock route, stock chart route, and index intelligence cards so verification, source, freshness, and seeded-vs-verified state are obvious to users and staff.
- Added a dedicated market-data handoff status layer inside external activation so provider URL, signed ingest, cron posture, and tester readiness are now visible from one launch-critical admin surface.
- Added first-target coverage tracking in Market Data Ops so Tata Motors quote, Tata Motors chart, and the tracked index snapshots now show exact verified/seeded/pending state from one admin view.
- Added real CTA paths to pricing and homepage plan cards so plan discovery now routes users into signup, onboarding, or plan comparison instead of dead-ending as a static brochure.
- Added a mobile-friendly header navigation layer with collapsible market, platform, workspace, and admin groups so the top navigation no longer depends on desktop-only multi-row layouts.
- Upgraded the global search into an assisted search flow with live suggestions in both the header and `/search`, so it behaves more like a navigation surface and less like a plain text box.
- Turned the first tool-detail routes into actual interactive utilities by adding working panels for position sizing, IPO lot calculation, and SIP goal planning instead of leaving the tool pages as description-only shells.
- Turned the screener from a static layout into an interactive workspace with clickable saved stacks, clickable metric groups, filtered result states, and direct handoff actions into stock pages, charts, and the user workspace.
- Upgraded the portfolio import, manual builder, and broker review routes into first-step interactive workflow panels so portfolio now behaves more like a real subscriber workflow instead of only describing the process.
- Rewrote more stale spec-style copy on the public portfolio, tools, and option-chain routes so they describe immediate user value instead of internal roadmap framing.
- Rewrote charts, screener, and mutual-fund hub copy to be more user-facing and action-oriented, reducing another layer of internal planning language on key public pages.
- Rewrote more signed-in and high-traffic route copy across account, watchlists, alerts, brokers, and stock detail so the protected workspace and stock pages feel less like build notes and more like a real product.
- Rewrote onboarding, learn, contact, and market-overview hub copy so these high-traffic entry routes now explain what users can do today instead of leaning on internal planning language.
- Rewrote trader-workstation, chart-layouts, saved-screens, and inbox copy so the power-user and subscriber workspace routes feel more product-ready and less like internal implementation notes.
- Rewrote billing, access, alerts, and portfolio import/manual sub-route copy so subscriber and retention workflows read more like live product experiences than staged implementation notes.
- Rewrote trader presets, scanner presets, and the workspace hub copy so repeat-use and power-user routes continue to feel more product-ready and less like planning language.
- Rewrote community, mentorship, and index-replay copy so these learning and workstation routes describe current user value more directly instead of reading like future-state framing.
- Rewrote pricing, webinars, newsletter, privacy, and terms copy so key public trust and conversion pages feel less placeholder-heavy while still staying honest about beta and launch posture.
- Rewrote the wealth-product hub plus ETF, PMS, AIF, and SIF listing copy so those category routes feel more like active discovery surfaces and less like reserved space for later expansion.
- Rewrote courses, search, mobile-readiness, and source-readiness copy so these route families explain current value and execution status more directly instead of leaning on beta-note or planning-heavy phrasing.
- Rewrote the IPO hubs, sectors metadata, and 404 fallback copy so these market-discovery routes feel clearer and less like internal system language.
- Rewrote more homepage and launch-readiness copy so the main public entry point and go-live messaging feel less shell-like and more product-facing without overstating launch completeness.
- Rewrote the course detail, webinar detail, and account setup routes so deeper onboarding and education pages use more direct user-facing language instead of roadmap-heavy phrasing.
- Rewrote more stock, mutual-fund, and IPO detail-page explanatory copy so core research routes keep reducing “should/later/eventually” phrasing without hiding real pending live-data gaps.
- Rewrote more residual support and workflow copy across indices, broker sync review, tool detail, get-started, alerts, and inbox so the remaining public/support routes feel more direct and less planning-heavy.
- Rewrote another final sweep across homepage, launch-readiness, search, charts, account setup, alerts, privacy, and terms so the remaining public edges use cleaner product-facing language with fewer residual beta-note phrases.
- Rewrote the stocks hub plus a few remaining homepage, privacy, and markets phrases so the last visible public-entry copy keeps moving away from placeholder or planning-heavy wording.
- Clarified pending market-data states on the stock and mutual-fund hub cards so users now see “awaiting verified feed” language instead of raw fallback/pending labels.
- Clarified pending market-data messaging on stock and mutual-fund detail pages plus the dedicated stock-chart route, so verified-vs-pending feed states read more cleanly while live upstream data is still being activated.

## Completed

### Planning and research

- Competitor and product research workbook created
- Google Sheets importable workbook generated
- Execution roadmap written
- Product direction narrowed to traffic plus subscription toolkit

### Foundation app

- Next.js app scaffold created
- Tailwind and shared UI components added
- Homepage created
- Pricing page created
- Login shell created
- Signup shell created
- Screener shell created
- Stock route family created
- IPO route family created
- Mutual fund route family created
- Sitemap, robots, manifest, and health endpoint created
- Build verified successfully

### Environment

- Node.js installed locally
- Project dependencies installed locally
- Package lock created for repeatable installs

### Phase 0.1 implementation

- Supabase SSR package added
- Server and browser Supabase helpers added
- Auth server actions added
- Login and signup pages upgraded from static shells to real forms
- Protected account route added
- Proxy session refresh file added
- Base SQL schema created
- Seed SQL created
- Official source registry file created

### Phase 1 starter work

- Added stock listing hub page
- Added IPO listing hub page
- Added mutual fund listing hub page
- Centralized richer content records for sample asset pages
- Upgraded detail pages with reusable summary and key-point blocks
- Added database-first content access layer with safe local fallbacks
- Connected stock, IPO, and fund routes to the shared content layer
- Added subscription and entitlement placeholders to the account page
- Added structured content-sections migration and seed data
- Added reusable content section renderer for asset pages
- Added protected admin content overview page
- Added source registry loader and trust cards for public asset pages
- Added methodology page for source and data-discipline transparency
- Added protected admin source registry page
- Added standardized page blueprints and coverage scoring rules
- Added coverage score cards to stock, IPO, and mutual-fund pages
- Upgraded admin content overview with page-completeness scoring
- Added editorial readiness guidance and next-action prompts
- Upgraded admin content overview with status labels and average score summaries
- Added unified pipeline ordering and priority queue logic
- Added admin priority and depth queues for content operations
- Added richer asset insight cards for stocks and funds
- Added compare-ready route structures for stocks and mutual funds
- Expanded public pages toward a research workflow, not just single-asset viewing
- Added breadcrumb UX and JSON-LD helpers for major public pages
- Improved SEO-readiness visibility in the admin dashboard
- Added sector hubs for grouped stock authority pages
- Added mutual fund category hubs for grouped discovery pages
- Linked asset pages into hub flows to strengthen cluster navigation
- Added a market overview page for daily discovery and habit formation
- Added reusable market stats and discovery components
- Added learn hub and evergreen glossary-style article pages
- Added results calendar page for event-led discovery
- Added Nifty50, BankNifty, and Sensex intelligence page foundation
- Added weighted component contribution logic for daily bullish/bearish interpretation
- Added a dedicated index intelligence hub page
- Added pullers and draggers cards for index pages
- Added public-vs-premium framing for the index product track
- Added Fin Nifty as the fourth tracker in the index cluster
- Added a plain-English build tracker page for macro roadmap visibility
- Added index sub-navigation so the four tracker pages behave like one product area
- Added intraday-style tracker timelines to the four index pages
- Added explicit public-vs-premium tracker feature framing
- Added trend labels and session-phase storytelling to the index intelligence model
- Added database migration and seed foundation for tracked indexes, component weights, snapshot history, and refresh runs
- Added a protected index-operations admin page for future realtime tracker rollout
- Added an Advanced Charts product page to hold the proprietary TradingView indicator track
- Added Advanced Charts into pricing, account planning, and the master roadmap
- Wired index pages to prefer persisted tracker records when Supabase data is available
- Added seed snapshot history for the four tracked indexes to support the persisted path
- Added database foundation for portfolios, holdings, imports, and broker connections
- Added a portfolio tracker product page covering CSV import, holdings, and broker-connectivity plans
- Added portfolio tracking into nav, sitemap, account planning, and the subscriber roadmap
- Added a source-readiness page so data, charting, and broker-access signups can start in parallel
- Documented chart strategy around Lightweight Charts, TradingView Advanced Charts, and your proprietary Pine Script
- Installed `lightweight-charts` and replaced the Advanced Charts placeholder with a real interactive chart workspace
- Added `/charts` as the main chart hub and `/stocks/[slug]/chart` as the dedicated stock chart route for chart-first users
- Reworked the header into a two-row menu with index-first top navigation, search, and login/signup entry points
- Added the main chart workspace directly onto stock landing pages while keeping the dedicated chart route for deeper workflows
- Documented the IPO-to-listed-stock lifecycle rule and the manual override/data-ops rule in the master plan
- Added explicit admin visibility for lifecycle handling and manual override strategy inside the content operations view
- Added a dedicated CMS operations page to define manual editing, uploads, override controls, and staff audit-trail requirements
- Set the current build-phase access model to Elite-equivalent for signed-up users while keeping admin/staff routes protected
- Added a protected revision-log page to model staff edit history, timestamps, reasons, and future rollback workflows
- Upgraded the IPO hub cards and build tracker so the current execution focus reflects richer stock/IPO pages and backend operations
- Added an SME IPO hub and separated mainboard vs SME framing inside the IPO system
- Added structured CMS page blueprints for stocks, mainboard IPOs, SME IPOs, and mutual funds so the future backend editor has clear block groups
- Added CMS implementation-status tracking so each page blueprint now shows what is live, partial, or still planned
- Added visible fundamentals and shareholding sections to stock pages so the CMS blueprint now maps to real research blocks
- Added return table, risk/category, holdings, sector allocation, and fund manager sections to mutual-fund pages
- Added visible subscription-watch, allotment-checklist, and listing-watch sections to IPO pages so the lifecycle route covers more than GMP and dates
- Added a dedicated override-operations admin page so manual source patches, owners, severity, and review dates have a clear staff workflow
- Added a dedicated document-operations admin page so uploads, filing ownership, review state, and reference assets have a visible CMS workflow
- Added a real courses route family with dummy data for free, bundled, and future premium learning products
- Added portfolio AI-validation and notification-readiness blocks so import mismatches and user communication are now part of the product model
- Added a dedicated AI-ops admin page for portfolio validation, smart-result search, and cross-channel alert planning
- Added a proper admin dashboard homepage so you and your team have one backend entry point across content, AI, sources, documents, overrides, and revisions
- Added a free-tools route family with dummy utilities to support user activation and signup intent without hard paywalls
- Added a smart-search route and routed the header search into structured results across stocks, IPOs, funds, tools, courses, and learn pages
- Added dedicated portfolio import-review and manual-builder routes so the portfolio product now has clearer user-flow foundations
- Added a public alerts hub and subscriber alert-preferences route so the notification strategy now exists as a real product layer, not only an admin plan
- Added an account inbox route so portfolio review tasks, IPO reminders, and future AI summaries now have a proper subscriber destination
- Added launch-readiness, privacy, terms, and contact routes so the app is moving closer to a credible public release surface
- Added a public get-started route and a protected account-setup route so both visitors and signed-in users have clearer onboarding paths
- Expanded the build tracker and masterplan phases so they now reflect the fuller blueprint scope: auth, data pipelines, trader workstation, wealth products, learning, AI, and mobile-readiness
- Reworked auth toward a launch-standard path with Google-first login, email verification fallback, and a dedicated Supabase callback route instead of password-first forms
- Added a launch-control admin page so coded work, blocked items, required credentials, and exact user-side inputs are visible in one place
- Added a system-status admin page so environment readiness for Supabase, auth, and payments can be checked directly inside the app
- Closed Phase 1 as the completed launch-shell milestone and moved real auth/data activation work into Phase 2 so the tracker reflects a cleaner execution boundary
- Added a DB activation admin page so migrations, seeds, and backend rollout order are visible inside the app during Phase 2
- Added a content-rollout admin page so each route family’s fallback-vs-database state is visible during the Phase 2 transition
- Added an API-access admin page so external accounts, approvals, and credentials can be collected in parallel during Phase 2
- Added a deployment-readiness admin page plus env placeholders for support email and Resend so go-live blockers are tracked more explicitly
- Added a communication-readiness admin page and made contact/privacy support messaging env-driven so launch support details can be activated cleanly
- Added an auth-activation admin page so Google login, email-link auth, callback URL setup, and auth testing are tracked in one place
- Added a payment-readiness admin page and subscriber billing workspace so Razorpay setup, webhook readiness, and future plan activation have a dedicated Phase 2 surface
- Added a subscription-matrix admin page so pricing, entitlements, and future gating decisions are tracked as a real operating surface before payments go live
- Added a signed Razorpay webhook route and shared billing-event catalog so the payment layer now has a real backend entry point instead of only admin planning surfaces
- Added a payment-events admin page so webhook event handling, billing-state samples, and entitlement follow-up rules are visible to the team
- Added a billing SQL foundation migration and seed so customers, plans, invoices, subscription events, and entitlement audits are ready for Supabase activation
- Added subscriber invoice-history and admin billing-ledger pages so billing state can now be reviewed from both the user and operations side
- Added an entitlement-audit admin page so access-level changes tied to billing events and support actions are now visible in the backend
- Added a platform-architecture admin page and explicit scale-architecture roadmap phase so thousands of dynamic plus editorial pages, plugin-style modules, and long-term SQL planning are now tracked as first-class work
- Added a content-model registry admin page so future CMS entities, workflow states, announcements, documents, and relationship planning are tracked before scale forces rework
- Added an editorial CMS SQL foundation migration and seed so manual blocks, announcements, documents, and workflow assignments now have real database groundwork
- Added an asset-relationships and taxonomy SQL foundation so future related-page linking, clustering, and lifecycle connections do not depend on hardcoded paths
- Added a relationships admin page so taxonomy and graph planning can now be reviewed as part of the live backend operating system
- Added an editorial-workflows admin page so draft, review, and publish-queue thinking is now part of the backend operating model
- Added a publishing-calendar admin page so scheduled release planning is now part of the editorial backend model
- Added an announcements admin page so manual company updates, IPO notes, and commentary can now be handled as a first-class editorial surface
- Added a lifecycle-operations admin page so IPO-to-stock transitions and archive continuity are now handled as an explicit backend system
- Added a source-jobs admin page so feed adapters, refresh cadence, and blocked ingest work are now visible in the backend operating layer
- Added a knowledge-ops admin page so the retrieval inputs for smart search and future AI features are now tracked as a real backend concern
- Added an asset-registry admin page so canonical asset ownership, aliases, and lifecycle-aware route continuity are now visible before the platform scales to thousands of public pages
- Added an asset-registry SQL foundation so canonical records and aliases now have real backend groundwork for page identity, imports, and lifecycle transitions
- Added a field-dictionary admin page so source-backed metrics, editorial fields, lifecycle enums, and derived-output boundaries can be reviewed before the CMS hardens
- Reworked the top navigation so public links stay visible while workspace and admin/backend routes now live under dropdown menus
- Added an explicit Phase 9 for one-click modules, integration marketplace thinking, and operator-grade extensibility so the WordPress-like platform goal is tracked clearly
- Added a block-editor admin page plus rollback-center page so the CMS is moving from planning-only views toward real editing and recovery workflows
- Added subscriber watchlists, saved screens, and broker-connections routes so Phase 3 has started moving from account placeholders into a real retained-workspace layer
- Added a workspace hub, access-model page, and broker-sync-review page so the subscriber layer now covers saved context, entitlement framing, and approval-first broker updates
- Added a trader-workstation route so Phase 4 has now started as a real product surface instead of remaining only a roadmap item
- Added dedicated option-chain and scanner-presets routes so the trader workstation is now becoming a real product cluster rather than a single landing page
- Added chart-layouts, index-replay, and trader-presets routes so the Phase 4 workstation now has chart memory, replay workflows, and saved trader setup foundations
- Added a Phase 2 execution SQL foundation for source contracts, ingest jobs, lifecycle transitions, override execution, and market-data readiness so the remaining activation work has durable backend structure
- Added a protected market-data ops page so latency promises, cache strategy, and licensed-realtime boundaries are now part of the live admin operating system
- Closed Phase 2 as the completed backend-foundation and execution-readiness milestone after filling the remaining source, lifecycle, override, and market-data groundwork
- Added a wealth hub plus ETF, PMS, AIF, and SIF route families so Phase 5 now exists as a real long-tail investor product layer
- Expanded investor tools with retirement, SWP, goal-allocation, and asset-location planning workflows to deepen the wealth utility stack
- Added a protected wealth-products admin page and extended CMS blueprints so new wealth families reuse the same CMS, registry, lifecycle, and document discipline
- Closed Phase 5 as the completed wealth-product expansion milestone
- Audited the roadmap against the original blueprint and expanded the tracker with missing creator-ops, AI-governance, CRM, subscriber-lifecycle, and support-operation phases
- Started Phase 6 with live webinar, newsletter, and creator-studio surfaces plus stronger learn and course positioning for creator-led publishing and distribution
- Added a low-cost AI operating layer in admin so formula-first workflows, optional live model usage, cost controls, and AI env readiness are now part of the real platform plan
- Added webinar detail routes and newsletter track detail routes so Phase 6 now has reusable creator and distribution route families instead of hub pages alone
- Added media-library and distribution-ops admin surfaces so creator assets, repurposing paths, and owned-distribution systems now have real operational coverage
- Added a public market-copilot route, AI guardrails admin surface, and smarter formula-first search answer layer so the first AI-ready product and governance milestone is now complete
- Started Phase 8 with a module-catalog admin surface and reusable field-packs surface so plugin-style platform expansion is now becoming a real operator workflow
- Added an operator-controls admin surface so module toggles, rollout modes, and future behavior settings can start moving out of code and into structured platform operations
- Started Phase 9 with an integration-marketplace admin surface so replaceable provider classes are now part of the live platform roadmap instead of staying only conceptual
- Added data-lineage and delivery-layer admin surfaces plus Phase 8 architecture SQL groundwork so traceability, derived outputs, and delivery artifacts are now formal platform systems
- Closed Phase 8 as the platform-operating-system milestone and moved the roadmap fully into Phase 9 extensibility work
- Added module-installer and provider-config admin surfaces so one-click starter kits and switchable provider profiles are now part of the real Phase 9 execution path
- Re-audited the full phase map, fixed a Phase 8 item-status inconsistency, and added a new Phase 11 for reliability, observability, security, and release-confidence work
- Added a reliability-ops admin surface so release QA, failure visibility, recovery planning, and production-confidence gaps are now tracked explicitly
- Started Phase 10 with CRM ops, campaign-engine, support-ops, portfolio-exception, and help-center surfaces so lifecycle growth and trust operations are now real product and admin tracks
- Added module-presets and lifecycle-campaigns admin surfaces so reusable expansion bundles and consent-aware growth journeys are now part of the live Phase 9 and Phase 10 execution path
- Added a provider-switchboard admin surface and a user-facing consent center so vendor fallback logic and messaging permissions are now part of the live extensibility and growth tracks
- Added provider-fallback and segment-playbook admin surfaces so resilience planning and lifecycle growth journeys now have dedicated operating layers in the admin stack
- Added provider-adapter and consent-ops admin surfaces so vendor contracts and permission governance now have dedicated backend operating layers
- Added provider-rollout and recovery-journey admin surfaces so rollout safety and trust-repair flows now have dedicated backend operating layers
- Added starter-kit and user-success admin surfaces so reusable expansion templates and subscriber-success workflows now have dedicated backend operating layers
- Added module-activation and campaign-insights admin surfaces so module readiness and lifecycle outcome reporting now have dedicated backend operating layers
- Added journey-governance admin surface and formally closed the current build-side extensibility and lifecycle-growth phases
- Started Phase 11 with access-governance, release-checks, observability, and recovery-readiness surfaces so reliability, security, operator access, and rollback discipline are now moving into real execution
- Expanded Phase 11 with cache-discipline and traffic-health surfaces so freshness rules, crawl integrity, schema quality, and route-health confidence are now part of the live reliability layer
- Added incident-response and performance-QA surfaces and formally closed the current reliability and production-confidence phase from the build side
- Started Phase 12 with mobile-readiness, mentorship, localization-studio, and design-system surfaces so app contracts, guided learning, multilingual creator expansion, and experience polish are now moving into execution
- Expanded Phase 12 with push-readiness and cohort-ops surfaces so mobile engagement and mentorship-style delivery now have dedicated operating layers too
- Expanded Phase 12 with mobile-journeys, language-rollouts, and experience-audit surfaces so cross-channel continuity, multilingual sequencing, and final polish now have dedicated execution layers too
- Added community-program and app-release-control surfaces and formally closed the current mobile/guided-learning/polish phase from the build side
- Added go-live-handoff and external-activation surfaces so the project can now move from build-complete status into real launch execution and owner-based activation planning
- Added launch-owner-matrix and launch-day-runbook surfaces so launch accountability and same-day sequence control now live inside the admin system instead of only in handoff notes
- Added launch-scope and preflight-checklist surfaces so the team can now decide what is truly public at launch and what final verification must happen after activation but before promotion
- Added post-build launch phases to the tracker so build-complete no longer falsely implies tweet-ready public launch, and added a launch-mode surface to keep internal review, launch prep, public beta, and full launch clearly separated
- Added a public launch-status banner, corrected stale footer messaging, and added a beta-gate surface so controlled launch exposure is now part of the real product and admin flow
- Added a launch-scorecard surface so the team now has a quick env-and-posture readiness read instead of relying only on scattered launch pages
- Added a domain-readiness surface so the team can now verify whether public URL, callback expectations, sitemap base, and support-contact trust are aligned for launch
- Added a callback-matrix surface so Supabase, Google, local, and production redirect values can be copied exactly instead of being reconstructed from memory during activation
- Added an activation-sequence surface so launch setup now has an exact step order across envs, callbacks, auth activation, redeploy, and preflight signoff
- Added a live-smoke-tests surface so the team now has a concrete post-activation verification run across login, protected routes, trust contact, optional billing, and public launch posture
- Added a launch-decision surface so the team can now see the highest safe launch mode the platform can honestly claim based on real blockers instead of optimism
- Added a go-no-go surface so the launch call can now be made from one summary view that combines blockers, readiness, activation order, smoke tests, and recommended mode
- Added a launch-approvals surface so final go-live accountability is now visible by owner lane instead of being assumed under deadline pressure
- Added a beta-command-center surface so Phase 14 now has a real operational bridge while Phase 13 remains the launch gate that still needs real activation and preflight execution
- Added a beta-feedback surface so the first wave of outside-user issues can now be organized by trust impact instead of being treated as loose notes after launch
- Added a beta-triage surface so early beta issues now have severity, owner, and immediate response rules instead of being handled ad hoc once outside users arrive
- Added a beta-invite-plan surface so the team now has a staged expansion model for widening access without outrunning support, trust, and smoke-tested confidence
- Added a beta-metrics surface so the team can now judge whether auth confidence, support response, trust breaks, and premium clarity are improving before beta expands again
- Added a credential-matrix surface so launch activation now includes an exact env-key inventory instead of relying on memory or scattered setup notes
- Re-audited the product against charting, stock-research, IPO, fund, wealth, and learning reference platforms and expanded the roadmap so tweet-ready launch now depends on reference-grade page depth instead of only route coverage
- Expanded the tracker again with a fuller Phase 15 and a new Phase 16 so chart depth, stock-page research depth, fund depth, IPO tracker depth, screener columns, wealth-product detail, and learning-library density are now explicit remaining work instead of vague future polish
- Added a protected reference-parity page so the missing depth across charts, stocks, funds, IPOs, wealth products, screeners, and learning can be reviewed inside admin and no longer gets forgotten between deadline pushes
- Started Phase 15 for real by deepening the charts hub and stock-detail route while keeping the build tracker honest that broad public launch still needs reference-grade product depth
- Corrected the launch-readiness logic so placeholder env values no longer count as real credentials, which makes the Phase 13 status more honest and keeps fake readiness from slipping into launch calls
- Closed Phase 13 as the controlled public-beta activation milestone by locking launch posture to public beta, using the current Vercel domain as the public site URL, tightening blocker logic, and moving the remaining real-world launch work into Phase 14 beta operations
- Closed Phase 14 from the build side by adding a beta-ops runbook and completing the beta command, feedback, triage, invite, metrics, and support-monitoring operating layer inside admin
- Deepened Phase 15 across screener, mutual fund detail, IPO detail, wealth-product pages, and option analytics so the public product now carries more research framing and less route-shell language while the tracker explicitly reflects those parity gains
- Deepened Phase 15 again across charts and stock-detail pages so chart controls, indicator framing, quote context, financial scorecards, and ownership/event blocks now feel materially closer to a real research product
- Closed Phase 15 from the build side by tightening public-beta launch messaging, pricing posture, announcement readiness, and the final reference-depth push across the main public product surfaces, while Phase 16 now becomes the active post-launch depth track
- Started Phase 16 for real by adding public learning tracks plus an admin learning-paths surface so education depth now has a visible sequenced product layer instead of only roadmap text
- Expanded Phase 16 again by adding a research-archive surface plus event-memory framing in results and announcements, so post-launch market context is now starting to behave like durable archive infrastructure instead of one-off notes
- Expanded Phase 16 again with a shared education-library model, a protected education-library admin surface, and denser public course/webinar framing so learning depth now behaves more like one connected library instead of isolated content hubs
- Expanded Phase 16 again with a shared mentorship-ladders model, a protected mentorship-ladders admin surface, and stronger public mentorship/community progression framing so guided learning now behaves more like a true ladder instead of separate stubs
- Expanded Phase 16 again with a shared replay-memory model, a protected replay-memory admin surface, and stronger newsletter/archive continuity so creator-led learning now preserves follow-through instead of ending as one-time drops
- Closed Phase 16 from the build side by adding an asset-memory map, a protected asset-memory admin surface, and stronger archive continuity across results, learning, newsletters, and product-family memory
- Added a source-mapping desk so the team now has one admin place to manage canonical asset sheets, trusted sources, editorial-template prep, and document intake for scale
- Replaced the stale hardcoded launch-readiness checklist with env-aware status logic so Supabase, support, and communication setup reflect the real current posture instead of placeholder pending states
- Wired the first delayed-data bridge for stocks and mutual funds so public pages can now prefer persisted source snapshots from Supabase before falling back to sample values
- Added a verified market-data ingestion path plus provider-sync execution layer so Tata Motors quotes, OHLCV bars, and tracked index snapshots can be persisted into the exact tables the public routes already read
- Added Vercel-cron-compatible provider sync with `CRON_SECRET` support plus a safe daily production schedule, so the platform now has a built-in end-of-day market refresh path even before higher-frequency paid cron is enabled
- Added a sample Tata Motors plus index payload endpoint and surfaced it in Source Jobs and External Activation so the remaining provider handoff work is now copy-pasteable instead of implicit
- Added a validation-only route plus a dedicated market-data playbook admin surface so provider payloads can be checked safely before they write into Tata Motors or index production tables
- Cleaned another shared public-language pass across source trust, chart workspace, footer, index timeline, account setup, and get-started so fewer global surfaces still read like internal foundation notes
- Tightened another chart-and-index wording pass so preview-mode chart states and index-ops messaging stay clearer while verified source feeds are still being activated
- Compacted the shared site header and tightened source-readiness labels so the global top-of-page experience feels less oversized and the execution queue reads more clearly
- Cleaned another public-language batch across search, alerts, replay, access, screener, inbox, and the stock-chart route so fewer user-facing pages still carry soft roadmap phrasing
- Tightened another product-language pass across tools, portfolio, courses, access, and terms so core utility, retention, education, and trust routes feel more launch-ready
- Tightened another detail-route pass across stock charts, course detail, portfolio import, and chart preview status so the main high-intent flows read more like live product surfaces
- Cleaned another detail-surface batch across stocks, funds, portfolio, screener, option chain, tools, and account messaging so fewer high-intent routes still expose preview-style labels
- Tightened another shared language pass across charts, market copilot, workspace, stocks, funds, and account state so more public surfaces read as active product instead of reference or preview layers
- Cleaned another edge-route batch across help, consents, methodology, and auth helper text so support, trust, and onboarding surfaces stay aligned with the product tone
- Tightened another trust-language pass across source trust, privacy, terms, market copilot, and stock ownership context so fewer public surfaces still read like transitional placeholder copy
- Simplified the stock and stock-chart hero navigation so the top of those pages no longer piles multiple action pills above the main quote and chart areas.

- Cleaned another public-facing copy batch across courses, indices, and the stock chart route so those surfaces read less like internal launch framing and more like active product experiences.

- Fixed the broken global header search behavior by turning compact-mode suggestions into a proper dropdown and hiding them until users actually type, so the header no longer expands into a giant multi-row layout.
- Reopened the roadmap beyond Phase 16 by adding explicit Phase 17 to Phase 20 execution tracks for live data, coverage scale, subscriber truth, and broad-public launch QA, plus a protected public-launch backlog page in admin so the pending work stays visible inside the product.
- Upgraded the build tracker and launch-readiness pages with a clearer post-Phase-16 execution summary, including active-vs-queued phase counts, direct backlog visibility, and a faster handoff into the public-launch backlog from launch surfaces.
- Added a canonical asset intake desk in admin with exact spreadsheet columns, first-wave targets, and coverage gaps for stocks, funds, IPOs, ETFs, PMS, AIF, and SIF so Phase 18 human execution can start in parallel with ongoing platform build work.
- Added a subscriber-launch readiness desk in admin so Phase 19 now has one place for plan gating truth, billing activation, webhook-backed state, support delivery, and conversion-path readiness instead of scattering those blockers across multiple ops pages.
- Added a public-launch QA desk in admin so Phase 20 now has one place for mobile quality, smoke tests, incident drills, launch messaging, and final go / no-go discipline instead of leaving broad-public launch review fragmented across multiple ops surfaces.
- Added a provider-onboarding desk in admin so Phase 17 now has one place for configuring the first trusted stock and index market-data feed, validating payloads, checking execution auth, and tracking the exact first rollout set.
- Added a search-and-screener truth desk in admin so Phase 18 now has one place for tracking the move from sample-driven suggestions and screener rows to canonical asset-backed search and source-backed metric truth.
- Added a conversion-path audit desk in admin so the broad-public journey from discovery to signup to account setup to paid conversion can be reviewed as one launch-critical sequence instead of as isolated routes.
- Added a trust-signoff desk in admin and raised the post-Phase-16 phase labels so the tracker now reflects the real backlog progress across live data, coverage scale, subscriber truth, and launch QA more honestly than the earlier all-zero execution view.
- Added a mobile-QA matrix desk in admin so the compact-screen launch pass now has one dedicated Phase 20 review surface covering discovery, search, charts, auth, and subscriber workflows.
- Added a real `/api/search/suggestions` path and moved the assisted search UI onto API-backed suggestions from the shared content layer instead of only using a static client-side suggestion list.
- Added a real `/api/index-snapshots` route plus live-refresh status cards on the indices hub and key index pages so Nifty 50, Sensex, Bank Nifty, and Fin Nifty now expose a browser-visible data-refresh path.
- Flattened the desktop header into a much leaner navigation model with an `Explore` menu and a small quick-link strip so the sticky header no longer behaves like a second page above the content.
- Expanded the seeded stock universe beyond Tata Motors and Reliance with additional bank, auto, IT, industrial, consumer, and pharma routes, and fixed broken screener stock links so the discovery flows now land on the correct stock pages.
- Reframed auth status messaging so login and signup no longer sound like a leftover Phase 0.1 setup note even while real provider activation still remains an external dashboard step.
- Added a protected `Demo Showcase Readiness` desk plus new tracker phases for tomorrow's walkthrough polish and the remaining external-activation work that still sits outside pure UI implementation.
- Rebuilt the stock and mutual-fund compare routes into richer decision pages, and added compare handoffs on the stock and fund hubs plus the stock and fund detail pages so tomorrow's demo path feels more intentional.
- Added reusable visual scorecards and timeline graphics to the stock page, mutual-fund page, compare routes, and index surfaces so the product now feels less text-only and more like a market dashboard.
- Added a protected `Launch Commitments` desk so Phase 22 now has one env-aware operator view for auth, payments, support, live-data activation, and broad-public trust commitments.
- Extended that Phase 22 truth layer onto the public launch-readiness and pricing pages too, so launch posture and billing honesty are visible outside admin and not buried only in operator screens.
- Reworked the public homepage into a more launch-ready market front door by removing phase-progress clutter, simplifying the header identity, replacing internal launch CTAs with stronger market-entry actions, and adding TradingView chart embeds so the first view feels like a live product instead of a roadmap surface.
- Added a unified Phase 17 to 20 execution board in admin and synced the execution-summary percentages to the tracker so live data, coverage scale, subscriber truth, and public launch QA can now be run from one operator surface instead of drifting across separate desks.
- Replaced the mixed chart layer with a shared TradingView widget system across the homepage, stock routes, stock chart pages, mutual-fund benchmark/proxy charts, and all four major index pages so the product now feels more visual and consistent wherever chart surfaces appear.
- Upgraded the screener from a fixed saved-stack view into a more demo-ready workflow with in-screen search, sector and truth-state filters, compare-only narrowing, and sortable route-backed results while keeping the truth layer explicit about which metrics are genuinely supported today.
- Expanded the first trusted market-data rollout beyond Tata Motors-only messaging, so provider onboarding, market-data ops, target registries, sample payloads, and route-level status cards now treat the completed stock set plus tracked indices as the real Phase 17 delayed-data handoff surface.

## Current next milestone

`Next milestone: launch execution, operator activation, and ongoing evidence-led optimization beyond the current completed phase map`

## Open tracks

1. Expand admin-side configuration panels so more features become operator-controlled without code edits
2. Grow the integration marketplace model so alerts, payments, brokers, storage, and AI providers stay replaceable
3. Turn field packs and route families into installable starter kits and reusable presets instead of only review surfaces
4. Add deeper provider-profile, adapter, and fallback logic so env keys, callbacks, failover paths, and rollbacks can be swapped safely later
5. Grow CRM segments, segment playbooks, lifecycle campaigns, consent controls, support ops, and portfolio exception workflows into richer operational systems
6. Keep the external activation checklist ready for real Supabase, auth, provider credentials, and optional AI provider activation
7. Keep the new reliability, observability, security, and release-confidence phase visible so launch pressure does not bury it
8. Upgrade charts, stocks, IPOs, funds, screeners, and wealth pages toward reference-grade depth before calling the platform broadly launch-ready

## Notes

- Realtime data must only be planned against legitimate sources and clear licensing.
- We should avoid adding random packages unless they directly support the master plan.
- Reusable layouts and data models should always come before feature-specific customization.
